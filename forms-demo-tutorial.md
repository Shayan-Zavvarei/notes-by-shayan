# جزوهٔ آموزشی — چگونه `forms-demo.html` ساخته شد

**از صفر تا نسخهٔ دیباگ‌شده، با تمام تصمیم‌ها، اشتباه‌ها و درس‌ها**

---

## فهرست

### بخش یک — مبانی فنی

- [فصل الف — HTML: چرا یک فایل کافی است](#فصل-الف--html-چرا-یک-فایل-کافی-است)
- [فصل ب — CSS: متغیر، آبشار، و چاپ](#فصل-ب--css-متغیر-آبشار-و-چاپ)
- [فصل ج — جاوااسکریپت: DOM، رویداد، وضعیت](#فصل-ج--جاوااسکریپت-dom-رویداد-وضعیت)
- [فصل د — پایتون: ابزار استخراج و راستی‌آزمایی](#فصل-د--پایتون-ابزار-استخراج-و-راستیآزمایی)

### بخش دو — ساخت پروژه

- [مقدمه — چه ساختیم و چرا](#مقدمه--چه-ساختیم-و-چرا)
- [فصل ۱ — استخراج مادهٔ خام از فایل‌های Word](#فصل-۱--استخراج-مادهٔ-خام-از-فایلهای-word)
- [فصل ۲ — معماری داده: چرا «توکن» و نه «فیلد»](#فصل-۲--معماری-داده-چرا-توکن-و-نه-فیلد)
- [فصل ۳ — سیستم طراحی و مسئلهٔ «کاغذ داخل برنامه»](#فصل-۳--سیستم-طراحی-و-مسئلهٔ-کاغذ-داخل-برنامه)
- [فصل ۴ — موتور رندر: یک تابع، دو خروجی](#فصل-۴--موتور-رندر-یک-تابع-دو-خروجی)
- [فصل ۵ — موتور گردش کار](#فصل-۵--موتور-گردش-کار)
- [فصل ۶ — چاپ و خروجی PDF](#فصل-۶--چاپ-و-خروجی-pdf)
- [فصل ۷ — تقویم شمسی](#فصل-۷--تقویم-شمسی)
- [فصل ۸ — امضای الکترونیکی](#فصل-۸--امضای-الکترونیکی)
- [فصل ۹ — فرم‌ساز از صفر: دو نسل](#فصل-۹--فرمساز-از-صفر-دو-نسل)
- [فصل ۱۰ — کالبدشکافی باگ‌ها (مهم‌ترین فصل)](#فصل-۱۰--کالبدشکافی-باگها-مهمترین-فصل)
- [فصل ۱۱ — روش تست](#فصل-۱۱--روش-تست)
- [فصل ۱۲ — اگر بخواهی از صفر بسازی: نقشهٔ راه](#فصل-۱۲--اگر-بخواهی-از-صفر-بسازی-نقشهٔ-راه)
- [پیوست الف — واژه‌نامهٔ کد](#پیوست-الف--واژهنامهٔ-کد)
- [پیوست ب — دستورهای پرکاربرد](#پیوست-ب--دستورهای-پرکاربرد)

---

> **چطور این جزوه را بخوانم؟**
> اگر HTML/CSS/JS و پایتون را می‌دانی، مستقیم برو به **بخش دو**. اگر می‌خواهی از پایه یاد بگیری،
> **بخش یک** را اول بخوان — آنجا فقط همان بخش‌هایی از هر زبان توضیح داده شده که در این پروژه
> واقعاً به کار رفته، با نمونهٔ واقعی از همین فایل.

---

# بخش یک — مبانی فنی

---

## فصل الف — HTML: چرا یک فایل کافی است

### HTML چیست و چه نقشی اینجا دارد

HTML زبان **ساختار** است، نه زبان برنامه‌نویسی. کارش این است که بگوید «این یک عنوان است، این
یک جدول است، این یک کادر ورودی است». مرورگر این ساختار را می‌خواند و به تصویر تبدیل می‌کند.

در این پروژه HTML سه کار می‌کند:

1. **پوستهٔ ثابت صفحه** — سربرگ، نوار تب، و ظرف‌های خالی
2. **کنترل‌های ورودی** — کادر متن، کشو، چک‌باکس، انتخاب فایل
3. **حامل داده** — از طریق صفت‌های `data-*` (که پایین‌تر می‌بینی)

نکتهٔ جالب: **بیشتر HTML این پروژه اصلاً در فایل نوشته نشده**. اگر بشماری، فقط ۶ عنصر ثابت در
فایل هست:

```
Counter({'div': 7, 'span': 3, 'p': 2, 'title': 1, 'h1': 1, 'footer': 1, 'hr': 1})
```

بقیه — تمام فرم‌ها، جدول‌ها، کنترل‌ها — **در لحظه با جاوااسکریپت ساخته می‌شوند**.

### ساختار حداقلی یک سند HTML

یک سند کامل معمولاً این شکل است:

```html
<!DOCTYPE html>
<html lang="fa" dir="rtl">
  <head>
    <meta charset="utf-8">
    <title>عنوان صفحه</title>
    <style> /* CSS */ </style>
  </head>
  <body>
    <!-- محتوا -->
    <script> /* JS */ </script>
  </body>
</html>
```

اما این فایل با `<title>` شروع می‌شود و بعد مستقیم `<style>` و محتوا. **چرا کار می‌کند؟**
چون مرورگر مدرن، سندی که این تگ‌ها را ندارد خودش با `<html>`، `<head>` و `<body>` می‌پیچد.
این «بازیابی خطا» (error recovery) بخشی از استاندارد HTML است.

### ولی یک تلهٔ واقعی اینجا وجود دارد: حالت Quirks

نبودن `<!DOCTYPE html>` بی‌ضرر **نیست**. بدون آن، مرورگر وارد **حالت Quirks** می‌شود — حالت
سازگاری با مرورگرهای دههٔ ۹۰:

```js
document.compatMode
// "BackCompat"  → حالت Quirks
// "CSS1Compat"  → حالت استاندارد
```

در حالت Quirks، قواعد CSS متفاوت‌اند. مثلاً **جدول‌ها ویژگی‌های متن را از والدشان ارث نمی‌برند**.

این دقیقاً همان چیزی است که **باگ ۲** (متن نامرئی سربرگ جدول) را ایجاد کرده بود. اثباتش را در
فصل ۱۰ می‌بینی — دو نسخهٔ یکسان ساختم، یکی با DOCTYPE و یکی بدون:

```
بدون DOCTYPE → thColor: rgb(239, 234, 227)   ← ارث نبرده، رنگ روشن حالت تیره
با  DOCTYPE → thColor: rgb(26, 26, 26)       ← درست ارث برده
```

> **درس:** همیشه `<!DOCTYPE html>` را بنویس. یک خط است و کل موتور رندر را در حالت درست
> قرار می‌دهد. نبودش باگ‌هایی می‌سازد که علتشان اصلاً به ذهن نمی‌رسد.

### راست‌چینی: `dir` و `lang`

```html
<html lang="fa" dir="rtl">
```

- `dir="rtl"` — جهت چیدمان از راست به چپ
- `lang="fa"` — به مرورگر می‌گوید زبان فارسی است (برای شکست خط، صفحه‌خوان، بررسی املا)

چون فایل ما تگ `<html>` صریح ندارد، این‌ها را با جاوااسکریپت تنظیم کردیم:

```js
document.documentElement.dir  = "rtl";
document.documentElement.lang = "fa";
```

`document.documentElement` همان عنصر `<html>` است — چه خودت نوشته باشی، چه مرورگر ساخته باشد.

### عناصری که در این پروژه استفاده شد

| عنصر | کارش | کجا استفاده شد |
| --- | --- | --- |
| `<div>` | ظرف عمومی، بدون معنی خاص | همه‌جا — کارت‌ها، ردیف‌ها |
| `<span>` | ظرف درون‌خطی (وسط متن) | جای خالی فرم، برچسب‌ها |
| `<p>` | پاراگراف | هر خط متن سند |
| `<h1>`، `<h2>` | عنوان‌ها (سلسله‌مراتب معنایی) | عنوان صفحه و کارت‌ها |
| `<table>` | داده‌های جدولی | **سربرگ سند** و جدول اقلام |
| `<input>` | ورودی تک‌خطی | متن، عدد، رادیو، چک‌باکس، فایل |
| `<textarea>` | ورودی چندخطی | متن بلند، ویرایشگر فرم‌ساز |
| `<select>` | کشوی انتخاب | تاریخ شمسی، نوع مرحله |
| `<button>` | دکمه | همهٔ کنش‌ها |
| `<img>` | تصویر | لوگو و امضا |
| `<footer>` | پابرگ (معنایی) | توضیح انتهای صفحه |

### چرا سربرگ سند `<table>` است و نه Flexbox؟

سؤال درستی است — امروز معمولاً برای چیدمان از Flexbox یا Grid استفاده می‌کنند و جدول فقط برای
داده. اما اینجا عمداً جدول انتخاب شد:

1. **سربرگ اصلی در Word یک جدول واقعی است** — با خطوط سلولی که باید عیناً بازتولید شوند
2. **`border-collapse` رفتار قابل‌پیش‌بینی در چاپ دارد** — خطوط جدول در PDF دقیق درمی‌آیند
3. **`table-layout:fixed` عرض ستون‌ها را قفل می‌کند** — سربرگ در همهٔ فرم‌ها یکسان می‌ماند

```css
.doc-header{ width:100%; border-collapse:collapse; table-layout:fixed }
.doc-header td{ border:1px solid #333; vertical-align:middle; padding:6px 10px }
.doc-header .dh-logo{ width:19% }
.doc-header .dh-title{ width:35% }
.doc-header .dh-meta{ width:27%; background:#FFF2CC }
.doc-header .dh-code{ width:19% }
```

> **قاعده:** اگر چیزی که می‌سازی **واقعاً جدول است** (سطر و ستون معنادار)، از `<table>` استفاده
> کن. اگر فقط می‌خواهی چیزها کنار هم بنشینند، از Flexbox استفاده کن.

### کنترل‌های فرم

آمار واقعی این فایل:

```
23 × type="button"     ← دکمه‌ها
 8 × type="text"       ← ورودی متنی
 1 × type="radio"      ← تک‌انتخابی (بقیه در JS ساخته می‌شوند)
 1 × type="file"       ← آپلود امضا
 1 × type="checkbox"   ← چندانتخابی
```

**نکتهٔ `type="button"`:** اگر `<button>` داخل یک `<form>` باشد و `type` نداشته باشد، پیش‌فرضش
`submit` است و صفحه را رفرش می‌کند. نوشتن `type="button"` این را خنثی می‌کند. عادت خوبی است که
همیشه بنویسی.

**رادیو در برابر چک‌باکس:**

```html
<!-- رادیو: فقط یکی از گروه انتخاب می‌شود — گروه با name یکسان تعریف می‌شود -->
<input type="radio" name="c-paymentMethod" value="نقدی">
<input type="radio" name="c-paymentMethod" value="واریز بانکی">

<!-- چک‌باکس: هر کدام مستقل -->
<input type="checkbox" value="SPEC">
<input type="checkbox" value="DATASHEET">
```

**الگوی کنترل پنهان.** کنترل‌های بومی مرورگر زشت‌اند و سخت استایل می‌گیرند. الگوی رایج: کنترل
واقعی را پنهان کن، ظاهر دلخواه را با `<span>` بساز، و همه را داخل `<label>` بگذار:

```html
<label class="doc-check">
  <input type="radio" name="c-key" value="نقدی">
  <span class="box">☐</span> نقدی
</label>
```

```css
.doc-check input{ position:absolute; opacity:0; width:0; height:0 }
```

چرا کار می‌کند؟ **کلیک روی `<label>` به کنترل داخلش منتقل می‌شود.** پس کنترل پنهان است ولی
کاملاً کار می‌کند — و دسترسی‌پذیری (صفحه‌خوان، کیبورد) هم حفظ می‌شود.

> ⚠️ **تلهٔ تست:** Playwright نمی‌تواند روی عنصری با `opacity:0` کلیک کند. باید روی `<label>`
> کلیک کنی:
> ```js
> await page.locator('.doc-check:has-text("نقدی")').click();   // ✓
> await page.locator('input[data-ckey="x"]').click();          // ✗ Timeout
> ```

### صفت‌های `data-*` — پل بین HTML و JS

این مهم‌ترین ویژگی HTML در این پروژه است. هر صفتی که با `data-` شروع شود، صفت سفارشی معتبر است:

```html
<input data-fkey="requesterName" value="علی رضایی">
<td data-role="rowtotal" data-row="0">۱۲,۰۰۰,۰۰۰</td>
<button data-decide="می‌گردد">می‌گردد</button>
```

و در جاوااسکریپت از طریق `dataset` خوانده می‌شود (خط تیره به camelCase تبدیل می‌شود):

```js
element.dataset.fkey   // "requesterName"
element.dataset.row    // "0"  ← همیشه رشته است، برای عدد باید + بگذاری: +el.dataset.row
element.dataset.decide // "می‌گردد"
```

**چرا این‌قدر مهم است؟** چون به جاوااسکریپت اجازه می‌دهد بپرسد «این عنصری که کاربر رویش کلیک
کرد، چه معنایی دارد؟» بدون اینکه لازم باشد ارجاع به تک‌تک عناصر را نگه دارد.

جدول کامل قراردادهای این پروژه:

| صفت | معنی |
| --- | --- |
| `data-fkey` | کلید فیلد متنی |
| `data-ckey` | کلید تک‌انتخابی |
| `data-mkey` | کلید چندانتخابی |
| `data-sigfile` | ورودی فایل امضا |
| `data-irow` / `data-icol` | سطر/ستون جدول اقلام |
| `data-role` | نقش عمومی عنصر (`jdate`، `rowtotal`، `itemjdate`…) |
| `data-decide` | مقدار دکمهٔ تصمیم |
| `data-formtab` | شناسهٔ تب فرم |
| `data-si` | شمارهٔ مرحله در فرم‌ساز |

### data URI — جاسازی فایل داخل HTML

معمولاً تصویر این‌طور می‌آید:

```html
<img src="logo.png">   <!-- نیاز به فایل جداگانه -->
```

اما ما فایل مستقل می‌خواستیم. راه‌حل: **خودِ محتوای فایل را داخل آدرس بگذار**:

```html
<img src="data:image/png;base64,iVBORw0KGgoAAAANSUhEUg...">
```

ساختار: `data:` + نوع MIME + `;base64,` + دادهٔ کدشده.

Base64 روشی است برای نمایش دادهٔ دودویی با ۶۴ کاراکتر متنی امن. هزینه‌اش: حجم حدود **۳۳٪ بیشتر**
می‌شود (لوگوی ۷۰ کیلوبایتی → ۹۴ کیلوبایت متن). برای یک لوگو ارزشش را دارد؛ برای ده تصویر بزرگ نه.

همین تکنیک برای امضای آپلودشده هم به کار رفت — `FileReader.readAsDataURL()` دقیقاً همین رشته
را تولید می‌کند، و چون رشته است، **در PDF هم بدون هیچ کار اضافه‌ای می‌آید**.

---

## فصل ب — CSS: متغیر، آبشار، و چاپ

### CSS چیست

CSS زبان **ظاهر** است. با «انتخابگر» می‌گوید کدام عناصر، و با «اعلان» می‌گوید چه ظاهری:

```css
.doc-para { font-size: 14px; line-height: 2.15 }
/*  ↑انتخابگر    ↑ویژگی    ↑مقدار                */
```

### متغیرهای CSS — ستون فقرات این پروژه

متغیر CSS (نام رسمی: custom property) با `--` شروع می‌شود و با `var()` خوانده می‌شود:

```css
:root{
  --accent: #A63D2E;
}
.btn.primary{
  background: var(--accent);
}
```

`:root` یعنی بالاترین عنصر سند (`<html>`). متغیری که آنجا تعریف شود، همه‌جا در دسترس است.

**چرا حیاتی بود؟** چون تم تیره فقط با تغییر مقدار متغیرها ساخته می‌شود، بدون بازنویسی حتی یک
قاعدهٔ استایل:

```css
:root{
  --ground:#FBFAF8;  --ink:#1C1A17;
}
@media (prefers-color-scheme: dark){
  :root{
    --ground:#141311;  --ink:#EFEAE3;   /* فقط مقادیر عوض شدند */
  }
}
body{ background:var(--ground); color:var(--ink) }   /* این قاعده هرگز تغییر نمی‌کند */
```

### متغیر با دامنهٔ محلی — ترفند «کاغذ»

متغیرها فقط در `:root` تعریف نمی‌شوند. **روی هر عنصری قابل تعریف‌اند و به فرزندانش ارث می‌رسند.**

این دقیقاً راه‌حل مسئلهٔ «کاغذ» بود:

```css
.paper{
  --p-accent:#A63D2E;        /* متغیر محلی — فقط داخل کاغذ */
  --p-accent-soft:#F2E4E0;
  --p-ok:#2E6F4E;
}
.doc-blank input:focus{ background:var(--p-accent-soft) }
```

حالا هرچقدر تم برنامه عوض شود، `--p-accent-soft` ثابت می‌ماند، چون داخل `.paper` دوباره تعریف
شده و تعریف نزدیک‌تر برنده است.

### آبشار و ویژگی (Cascade & Specificity)

وقتی دو قاعده به یک عنصر می‌رسند، کدام برنده است؟ ترتیب تصمیم‌گیری:

1. **ویژگی بالاتر برنده است**
2. اگر ویژگی برابر بود، **آخری در فایل برنده است**

ویژگی این‌طور شمرده می‌شود (از قوی به ضعیف):

| نوع انتخابگر | مثال | وزن |
| --- | --- | --- |
| شناسه | `#print-root` | ۱-۰-۰ |
| کلاس / صفت / شبه‌کلاس | `.doc-para`، `[data-si]`، `:focus` | ۰-۱-۰ |
| نوع عنصر | `table`، `td` | ۰-۰-۱ |

مثال از همین پروژه:

```css
.scratch-preview-card .dh-title{ ... }   /* ۰-۲-۰ → دو کلاس */
.doc-header .dh-title{ ... }             /* ۰-۲-۰ → دو کلاس — برابر! */
```

هر دو ۰-۲-۰ هستند، پس **ترتیب در فایل تعیین‌کننده است** — و این دقیقاً **باگ ۷** بود: قاعدهٔ من
زودتر نوشته شده بود، پس قاعدهٔ پایه رویش می‌نشست و اصلاً اثر نمی‌کرد.

> **درس:** وقتی استایلی اعمال نمی‌شود، اول با ابزار توسعه‌دهنده (یا `getComputedStyle`) ببین
> واقعاً چه مقداری اعمال شده. سپس ویژگی و ترتیب را بررسی کن. `!important` آخرین راه‌حل است،
> نه اولین.

```js
// راه تشخیص برنامه‌نویسانه
getComputedStyle(document.querySelector('.dh-title')).fontSize   // "15px" ← نه ۲۰
```

### انتخابگرهایی که در این پروژه به کار رفت

```css
.card                      /* کلاس */
.paper table               /* نوادگان: هر table داخل paper */
body > *:not(#print-root)  /* فرزند مستقیم body، به‌جز print-root */
.doc-stage:last-child      /* آخرین فرزند */
.doc-blank:not(.empty)     /* که کلاس empty ندارد */
.status label.s-sent[data-on="1"]   /* ترکیب کلاس و صفت */
tr:last-child td           /* سلول‌های آخرین سطر */
```

`:not()` در دو جای کلیدی استفاده شد:

```css
@media print{
  body > *:not(#print-root){ display:none }   /* همه‌چیز جز ناحیهٔ چاپ */
  .doc-blank:not(.empty){ border-bottom:none } /* فقط زیر فیلدهای پرشده */
}
```

### چیدمان: Flexbox و Grid

**Flexbox** برای چیدمان یک‌بعدی (یک ردیف یا یک ستون):

```css
.sc-toolbar{
  display:flex;
  flex-wrap:wrap;      /* اگر جا نشد برود خط بعد */
  align-items:center;  /* هم‌ترازی عمودی */
  gap:6px;             /* فاصله بین آیتم‌ها */
}
```

**Grid** برای چیدمان دوبعدی یا ستون‌های با عرض مشخص:

```css
.scratch-grid{
  display:grid;
  grid-template-columns: 340px minmax(0,1fr);   /* ستون اول ثابت، دوم بقیهٔ فضا */
  gap:24px;
  align-items:start;
}
```

**نکتهٔ `minmax(0,1fr)`:** `1fr` یعنی «بقیهٔ فضا»، ولی به‌تنهایی حداقل عرضش برابر محتوایش است و
می‌تواند سرریز کند. `minmax(0,1fr)` اجازه می‌دهد کوچک‌تر از محتوا هم بشود — که برای ستونی که
جدول قابل‌اسکرول دارد ضروری است.

### واکنش‌گرایی

```css
@media (max-width: 960px){
  .scratch-grid{ grid-template-columns: 1fr }   /* در صفحهٔ کوچک، تک‌ستونه */
}
```

### چاپ

```css
@media print{
  body > *:not(#print-root){ display:none }
  #print-root{ display:block }
  .doc-stage{ break-inside:avoid; page-break-inside:avoid }
}

@page{ size: 210mm 297mm; margin: 10mm }
```

- `@media print` — قواعدی که فقط هنگام چاپ اعمال می‌شوند
- `@page` — تنظیم خودِ کاغذ (اندازه و حاشیه)
- `break-inside:avoid` — این عنصر وسطش نشکند و به صفحهٔ بعد نرود

> **`display:none` در برابر `visibility:hidden`:** اولی عنصر را کامل از چیدمان حذف می‌کند،
> دومی نامرئی می‌کند ولی **فضایش باقی می‌ماند**. در چاپ، این تفاوت یعنی صفحهٔ خالی — که دقیقاً
> **باگ ۴** بود.

### `color-scheme`

```css
.paper{ color-scheme: light }
```

این ویژگی به مرورگر می‌گوید «این ناحیه را با پالت روشن بکش». تأثیرش روی کنترل‌های بومی (کشو،
چک‌باکس، نوار اسکرول) و بعضی رفتارهای ارث‌بری است. برای ناحیه‌ای که باید همیشه «کاغذ سفید» باشد،
ابزار درستی است.

### واحدها

| واحد | معنی | کجا |
| --- | --- | --- |
| `px` | پیکسل ثابت | اندازهٔ قلم، حاشیه |
| `mm` | میلی‌متر (فیزیکی) | اندازهٔ کاغذ در `@page` |
| `%` | درصدی از والد | عرض ستون‌های سربرگ |
| `fr` | سهمی از فضای باقی‌مانده (فقط Grid) | ستون‌ها |
| `em` | ضریبی از اندازهٔ قلم عنصر | فاصله‌های نسبی |
| `ch` | عرض رقم صفر در قلم فعلی | محدودکردن عرض متن خوانا |

---

## فصل ج — جاوااسکریپت: DOM، رویداد، وضعیت

### نقش جاوااسکریپت اینجا

اگر HTML اسکلت و CSS پوست است، جاوااسکریپت **رفتار** است. در این پروژه تقریباً **همهٔ** رابط
کاربری با جاوااسکریپت ساخته می‌شود.

### تولید HTML با template literal

رشته‌های بین بک‌تیک (`` ` ``) می‌توانند چندخطی باشند و `${}` داخلشان کد اجرا کند:

```js
const name = "علی";
const html = `<div class="card">
  <h2>${name}</h2>
  <p>${count > 0 ? "دارد" : "ندارد"}</p>
</div>`;
```

این تکنیک ستون فقرات رندر این پروژه است. مزیتش سادگی است؛ عیبش این است که باید **دستی امن‌سازی**
کنی.

### `esc()` — چرا حیاتی است

```js
const esc = s => String(s==null ? "" : s)
  .replace(/[&<>"]/g, c => ({"&":"&amp;","<":"&lt;",">":"&gt;",'"':"&quot;"}[c]));
```

اگر کاربر در فیلد نام بنویسد:

```
<script>alert('نفوذ')</script>
```

و تو مستقیم داخل HTML بگذاری، مرورگر آن را **کد اجرا می‌کند**. به این حمله XSS می‌گویند.
`esc()` کاراکترهای خطرناک را به معادل بی‌خطرشان تبدیل می‌کند تا به‌عنوان **متن** نمایش داده شوند.

> **قاعدهٔ آهنین:** هر مقداری که از کاربر آمده و داخل HTML می‌رود، باید از `esc()` بگذرد.
> در این پروژه تنها استثنا `LOGO_SRC` و امضاهاست که خودمان ساخته‌ایمشان.

### `CSS.escape()` — همان مسئله، جای دیگر

وقتی مقدار متغیر را داخل **انتخابگر** می‌گذاری:

```js
// خطرناک: اگر key کاراکتر خاص داشته باشد، انتخابگر می‌شکند
document.querySelector(`[data-fkey="${key}"]`);

// امن
document.querySelector(`[data-fkey="${CSS.escape(key)}"]`);
```

### DOM — درخت سند

مرورگر HTML را به یک درخت شیء تبدیل می‌کند به نام DOM. جاوااسکریپت با این درخت کار می‌کند:

```js
document.getElementById("shell")           // با شناسه
document.querySelector(".paper")           // اولین تطابق با انتخابگر CSS
document.querySelectorAll("[data-role]")   // همهٔ تطابق‌ها (NodeList)

el.innerHTML = "<p>سلام</p>";              // جایگزینی کل محتوا
el.textContent = "متن ساده";               // فقط متن (امن، بدون تفسیر HTML)
el.classList.toggle("done", condition);    // افزودن/حذف کلاس
el.dataset.row                             // خواندن data-row
el.closest(".sc-stage")                    // نزدیک‌ترین والد با این انتخابگر
el.getBoundingClientRect()                 // ابعاد و موقعیت واقعی
```

**`innerHTML` در برابر `textContent`:** اولی HTML را تفسیر می‌کند (قدرتمند ولی نیازمند `esc`)،
دومی همه‌چیز را متن ساده می‌بیند (امن). در `updateItemTotals` عمداً از `textContent` استفاده شد.

### تفویض رویداد — مهم‌ترین الگوی این پروژه

روش ساده‌لوحانه: به هر دکمه یک شنونده وصل کن.

```js
// ✗ مشکل: بعد از هر بازسازی، باید دوباره وصل کنی
document.querySelectorAll('.btn').forEach(b => b.addEventListener('click', handler));
```

**مشکل بزرگ:** وقتی `innerHTML` را عوض می‌کنی، عناصر قدیمی نابود می‌شوند و شنونده‌هایشان هم
از بین می‌رود. عناصر جدید هیچ شنونده‌ای ندارند.

**راه‌حل: تفویض رویداد.** شنونده را یک بار روی `document` بگذار و در لحظهٔ رویداد بپرس «روی چه
چیزی کلیک شد؟»

```js
document.addEventListener("click", e => {
  const tab = e.target.closest("[data-formtab]");
  if (tab){ activeFormId = tab.dataset.formtab; render(); return; }

  const dec = e.target.closest("[data-decide]");
  if (dec){ /* … */ return; }

  if (e.target.id === "printBtn"){ /* … */ return; }
});
```

این کار می‌کند چون رویدادها **حباب می‌کنند** (bubble) — از عنصر کلیک‌شده تا ریشهٔ سند بالا
می‌روند. پس شنوندهٔ `document` همهٔ کلیک‌ها را می‌بیند.

کل این پروژه فقط **۳ شنوندهٔ سطح سند** دارد:

```js
document.addEventListener("click",  e => { … });   // کنش‌ها
document.addEventListener("input",  e => { … });   // تایپ
document.addEventListener("change", e => { … });   // انتخاب/فایل
```

**`e.target.closest(selector)`** ابزار کلیدی است: از عنصر کلیک‌شده شروع می‌کند و به سمت بالا
می‌گردد تا اولین عنصری که با انتخابگر می‌خواند. لازم است چون ممکن است کاربر روی `<span>` داخل
دکمه کلیک کرده باشد، نه خود دکمه.

### `input` در برابر `change`

| رویداد | چه زمانی | استفاده اینجا |
| --- | --- | --- |
| `input` | **با هر حرف** تایپ‌شده | فیلدهای متنی، تکس‌تاریا |
| `change` | وقتی مقدار **نهایی** شد | کشو، رادیو، چک‌باکس، ورودی فایل |

این تفکیک مهم است: برای کشوی تاریخ از `change` استفاده کردیم (چون تا انتخاب کامل نشود معنی
ندارد)، ولی برای تکس‌تاریای فرم‌ساز از `input` (چون پیش‌نمایش باید زنده باشد).

### الگوی وضعیت ← رندر

معماری کل برنامه این است:

```
وضعیت (state)  →  render()  →  DOM
      ↑                          │
      └──── رویداد کاربر ────────┘
```

```js
function render(){
  renderTabs();
  const form = getForm(activeFormId);
  const inst = getInstance(activeFormId);
  document.getElementById("shell").innerHTML =
    activeView === "run" ? renderRunView(form, inst) : renderBuilderView(form, inst);
}
```

هیچ‌جا DOM را «کمی» تغییر نمی‌دهیم؛ **وضعیت را عوض می‌کنیم و کل نما را از نو می‌سازیم**. ساده،
قابل‌پیش‌بینی، بدون ناهماهنگی.

### استثنای حیاتی این الگو

اما این الگو **یک استثنای اجباری** دارد که ندانستنش باگ ۵ را ساخت:

> **اگر کاربر در حال تایپ داخل عنصری است، آن عنصر را نباید بازسازی کنی.**

چون بازسازی یعنی عنصر قدیمی حذف و عنصر جدید جایش می‌نشیند → فوکوس می‌پرد → تایپ می‌شکند.

راه‌حل: در رویداد `input`، به‌جای `render()` کامل، فقط چیزهای **دیگر** را به‌روز کن:

```js
// ✗ باگ‌ساز
document.querySelector(".item-tbl-wrap").outerHTML = renderItemTable(inst, true);

// ✓ درست — فقط سلول‌های محاسباتی
function updateItemTotals(inst){
  (inst.items||[]).forEach((row, ri) => {
    const cell = document.querySelector(`[data-role="rowtotal"][data-row="${ri}"]`);
    if (cell) cell.textContent = itemTotal(row) ? fa(...) : "—";
  });
}
```

### ذخیره‌سازی محلی

```js
const STORE_KEY = "rocksit-forms-demo-v1";

// خواندن — همیشه داخل try، چون ممکن است JSON خراب باشد
let store = {};
try { store = JSON.parse(localStorage.getItem(STORE_KEY) || "{}"); }
catch(e){ store = {}; }

// نوشتن — هم داخل try، چون ممکن است فضا پر باشد
function saveStore(){
  try { localStorage.setItem(STORE_KEY, JSON.stringify(store)); } catch(e){}
}
```

`localStorage` فقط **رشته** ذخیره می‌کند، پس با `JSON.stringify` / `JSON.parse` تبدیل می‌کنیم.
ظرفیتش حدود ۵ تا ۱۰ مگابایت است و مخصوص همان دامنه/فایل است.

> نکته: چون امضاها به‌صورت data URI ذخیره می‌شوند، حجمشان در `localStorage` می‌رود. برای دمو
> مشکلی نیست؛ در محصول واقعی باید در دیتابیس ذخیره شوند.

### خواندن فایل

```js
const reader = new FileReader();
reader.onload = () => {
  inst.values[key] = reader.result;    // "data:image/png;base64,..."
  saveStore(); render();
};
reader.readAsDataURL(file);
```

`FileReader` **ناهم‌زمان** است — کد بلافاصله ادامه پیدا می‌کند و `onload` بعداً صدا زده می‌شود.
به همین دلیل `render()` داخل `onload` است، نه بعد از آن.

### دانلود فایل بدون سرور

```js
function download(name, text, type){
  const blob = new Blob(["﻿" + text], {type: type + ";charset=utf-8"});
  const a = document.createElement("a");
  a.href = URL.createObjectURL(blob);
  a.download = name;
  document.body.appendChild(a); a.click();
  setTimeout(() => { URL.revokeObjectURL(a.href); a.remove(); }, 500);
}
```

- `Blob` — یک شیء شبه‌فایل در حافظه
- `URL.createObjectURL` — آدرس موقت برایش می‌سازد
- `﻿` — نشانگر BOM؛ باعث می‌شود Excel فایل UTF-8 فارسی را درست باز کند
- `revokeObjectURL` — آزادکردن حافظه بعد از دانلود

### عبارت باقاعده (Regex)

در این پروژه چند جای کلیدی استفاده شد:

```js
// تبدیل ارقام فارسی
const faToEn = s => String(s).replace(/[۰-۹]/g, d => "۰۱۲۳۴۵۶۷۸۹".indexOf(d));

// تجزیهٔ نشانه‌های فرم‌ساز
const SCRATCH_MARKER_RE = /\[\[([^\]|*]+?)(\*)?(?:\|([a-zA-Z]+)(?::([^\]]+))?)?\]\]/g;
```

تشریح الگوی دوم:

| بخش | معنی |
| --- | --- |
| `\[\[` | دو براکت باز (با `\` فرار داده شده چون `[` معنی خاص دارد) |
| `([^\]\|*]+?)` | گروه ۱ = برچسب؛ هر چیزی جز `]`، `\|`، `*` — با `?` یعنی حریص نباشد |
| `(\*)?` | گروه ۲ = ستارهٔ اختیاری (اجباری‌بودن) |
| `(?:\|(...))?` | گروه بدون‌ثبت؛ کل بخش نوع اختیاری است |
| `([a-zA-Z]+)` | گروه ۳ = نام نوع |
| `(?::([^\]]+))?` | گروه ۴ = جزئیات بعد از `:` |
| `\]\]` | دو براکت بسته |
| `/g` | سراسری — همهٔ تطابق‌ها، نه فقط اولی |

> ⚠️ **تلهٔ `/g`:** عبارت‌های سراسری خاصیت `lastIndex` دارند که بین فراخوانی‌ها **حفظ می‌شود**.
> اگر ریست نکنی، بار دوم از وسط شروع می‌کند:
> ```js
> SCRATCH_MARKER_RE.lastIndex = 0;   // حتماً قبل از حلقه
> while ((m = SCRATCH_MARKER_RE.exec(text))) { … }
> ```

### الگوهای مدرن جاوااسکریپت که استفاده شد

```js
// تابع پیکانی
const fa = n => String(n).replace(...);

// پارامتر پیش‌فرض و spread
const F = (key, label, type, opts) => ({k:'f', key, label, type, ...opts});

// زنجیرهٔ اختیاری — اگر onReject نبود، خطا نمی‌دهد
if (st.decision.onReject?.type === 'loop'){ … }

// تخریب‌سازی
const { runs, decisionMeta } = parseScratchText(text, i);
const [jy, jm, jd] = toJalali(y, m, d);

// عملگر ادغام با ارزش‌های falsy
const type = (m[3] || 'text').toLowerCase();

// متدهای آرایه
runs.filter(r => r.k === 'f')
    .map(r => r.key)
    .forEach(k => …);
requiredKeys.every(k => values[k] != null);
form.stages.findIndex(s => s.key === target);
```

---

## فصل د — پایتون: ابزار استخراج و راستی‌آزمایی

### چرا پایتون؟

جاوااسکریپت در مرورگر اجرا می‌شود و به فایل‌های سیستم دسترسی ندارد. برای کارهای بیرون از
مرورگر — خواندن `.docx`، ساخت فایل نهایی، بررسی سلامت — به زبانی نیاز داشتیم که روی سیستم
اجرا شود. پایتون انتخاب شد چون:

1. کتابخانهٔ `python-docx` برای خواندن Word موجود است
2. کار با بایت‌های خام آسان است (که در باگ NUL نجات‌دهنده بود)
3. از قبل روی سیستم نصب است

### نقش پایتون در این پروژه

| کار | چرا پایتون |
| --- | --- |
| خواندن `.docx` | تنها کتابخانهٔ در دسترس |
| استخراج لوگو و Base64 | ماژول `base64` استاندارد |
| ساخت فایل نهایی از قالب | جایگزینی متنی ساده |
| بررسی بایت خالی | کار مستقیم با بایت |
| استخراج `<script>` برای بررسی نحو | regex روی فایل |
| شمارش صفحات PDF | regex روی بایت‌های PDF |
| **اصلاح جراحی فایل خراب** | تنها راه ممکن |

### محیط مجازی و PEP 668

```bash
$ pip install python-docx
error: externally-managed-environment
```

توزیع‌های جدید لینوکس (طبق PEP 668) اجازه نمی‌دهند با `pip` روی پایتون سیستم نصب کنی، چون
ممکن است بستهٔ سیستمی را خراب کند.

**راه درست: محیط مجازی**

```bash
python3 -m venv /tmp/docxenv          # ساخت محیط جدا
/tmp/docxenv/bin/pip install python-docx
/tmp/docxenv/bin/python3 script.py    # اجرا با پایتونِ همان محیط
```

محیط مجازی یک پوشه است با پایتون و بسته‌های خودش، کاملاً جدا از سیستم. حذفش هم فقط پاک‌کردن
پوشه است.

### ساختار واقعی `.docx`

یک نکتهٔ مهم: **فایل Word یک آرشیو ZIP است.**

```bash
$ unzip -l TOA-23.docx
  word/document.xml         ← متن اصلی
  word/styles.xml           ← تعریف سبک‌ها
  word/theme/theme1.xml     ← فونت‌های قالب
  word/media/image1.png     ← تصاویر
  word/header1.xml          ← سربرگ
```

پس اگر کتابخانه نداشتی هم می‌توانی مستقیم بخوانی:

```bash
unzip -p TOA-23.docx word/document.xml | grep -oE '<w:t[^>]*>[^<]*</w:t>' | sed 's/<[^>]*>//g'
```

یا با پایتون خالص:

```python
import zipfile
z = zipfile.ZipFile("TOA-23.docx")
xml = z.read("word/document.xml").decode("utf-8")
```

از همین روش برای پیدا کردن فونت استفاده شد:

```python
import re
m = re.search(r'<w:rFonts[^/]*/>', run_xml)
# نتیجه: <w:rFonts w:cs="B Nazanin"/>
```

### فضای‌نام XML

XML آفیس از فضای‌نام استفاده می‌کند — یعنی تگ‌ها پیشوند دارند:

```xml
<w:p xmlns:w="http://schemas.openxmlformats.org/wordprocessingml/2006/main">
```

در پایتون، وقتی با `lxml` کار می‌کنی، نام کامل تگ این شکلی است:

```python
'{http://schemas.openxmlformats.org/wordprocessingml/2006/main}p'
```

به همین دلیل کد ما این‌طور بررسی می‌کند:

```python
if child.tag.endswith('}p'):    # پاراگراف
elif child.tag.endswith('}tbl'): # جدول
```

`endswith` ساده‌ترین راه فرار از فضای‌نام طولانی است.

### واحد اندازه‌گیری آفیس: EMU

آفیس طول‌ها را با واحد EMU ذخیره می‌کند (English Metric Unit):

```
۹۱۴۴۰۰ EMU = ۱ اینچ
```

```python
print(sec.page_width / 914400, "inch")   # 8.27 → یعنی A4
```

### بایت در برابر متن — درسی که باگ NUL داد

پایتون دو حالت باز کردن فایل دارد:

```python
open(path, encoding="utf-8")   # حالت متن → رشته
open(path, "rb")               # حالت باینری → بایت
```

وقتی فایل خراب است (مثلاً بایت خالی دارد)، **حتماً** باید حالت باینری استفاده کنی:

```python
data = open(path, "rb").read()
print('has NUL:', b'\x00' in data)

idx = data.find(b'\x00')
print(repr(data[idx-80 : idx+80]))    # زمینهٔ اطراف مشکل
```

و برای اصلاح جراحی:

```python
start = data.find(b'function sanitizePath')
end   = data.find(b'\n}', start) + 2
old   = data[start:end]

new = (b'function sanitizePath(seg){\n'
       b'  return String(seg).replace(/[\\\\/:*?"<>|]/g, "-")...\n}')

assert old.count(b'\x00') == 1, "انتظار دقیقاً یک بایت خالی"
patched = data[:start] + new + data[end:]
assert b'\x00' not in patched, "هنوز بایت خالی هست"

open(path, "wb").write(patched)
```

> **دو `assert` کلیدی:** یکی قبل («آیا واقعاً همان چیزی است که فکر می‌کنم؟») و یکی بعد
> («آیا واقعاً درست شد؟»). این عادت جلوی خرابکاری کورکورانه روی فایل را می‌گیرد.

### فرارِ بک‌اسلش — منشأ همان باگ

در رشتهٔ بایتی پایتون، `\\\\` یعنی **دو** بک‌اسلش واقعی:

| در کد پایتون | بایت واقعی | در جاوااسکریپت خوانده می‌شود |
| --- | --- | --- |
| `b'\\'` | `\` | فرار |
| `b'\\\\'` | `\\` | یک بک‌اسلش ادبی |
| `'\x00'` | بایت صفر ← **فاجعه** | — |
| `'\\x00'` | `\x00` (چهار کاراکتر) ← درست | فرار در regex |

باگ دقیقاً همین بود: `\x00` به بایت واقعی تبدیل شد به‌جای اینکه چهار کاراکتر بماند.

**راه‌حل بهتر:** اصلاً از کاراکتر کنترلی در الگو استفاده نکن:

```js
/[\\/:*?"<>|]/g          // ✓ بدون کاراکتر کنترلی
/[\\/:*?"<>|\x00-\x1F]/g // ✗ ریسک‌دار در انتقال بین ابزارها
```

### ساخت فایل نهایی از قالب

```python
base = "/path/to/scratchpad"
template = open(f"{base}/forms-demo-template.html", encoding="utf-8").read()
forms_js = open(f"{base}/forms-data.js",           encoding="utf-8").read()
logo_b64 = open(f"{base}/logo_b64.txt",            encoding="utf-8").read().strip()

assert "__FORMS_JS__"    in template
assert "__LOGO_BASE64__" in template

out = template.replace("__FORMS_JS__", forms_js) \
              .replace("__LOGO_BASE64__", logo_b64)

open("forms-demo.html", "w", encoding="utf-8").write(out)
print("written, size:", len(out))
```

**چرا `assert`؟** اگر نشانه‌گذار در قالب نبود (مثلاً تایپی داشت)، جایگزینی بی‌صدا انجام نمی‌شد
و فایلی تولید می‌شد که ظاهراً سالم است ولی لوگو ندارد. `assert` این را به خطای فوری تبدیل می‌کند.

### استخراج جاوااسکریپت برای بررسی نحو

```python
import re
html = open("forms-demo.html", encoding="utf-8").read()
m = re.search(r"<script>(.*)</script>", html, re.S)   # re.S یعنی . خط جدید را هم بگیرد
open("/tmp/check.js", "w", encoding="utf-8").write(m.group(1))
```

سپس:

```bash
node --check /tmp/check.js
```

### شمارش صفحات PDF

فایل PDF ساختار متنی نیمه‌خوانا دارد. هر صفحه یک شیء `/Type /Page` است:

```python
import re
data = open("out.pdf", "rb").read()
pages = len(re.findall(rb'/Type\s*/Page[^s]', data))
print("pages:", pages)
```

- پیشوند `rb` = رشتهٔ بایتیِ خام (raw bytes)
- `\s*` = فاصلهٔ اختیاری (بعضی تولیدکننده‌ها فاصله می‌گذارند، بعضی نه)
- `[^s]` = جلوگیری از تطابق با `/Type /Pages` که شیء **مجموعهٔ** صفحات است، نه یک صفحه

### اجرای اسکریپت درون‌خطی

الگویی که در کل کار زیاد استفاده شد:

```bash
python3 - <<'PY'
import re
html = open("forms-demo.html", encoding="utf-8").read()
print(len(html))
PY
```

`<<'PY'` یعنی «تا خط `PY` را به‌عنوان ورودی بده». **کوتیشن دور `PY` مهم است** — بدون آن، پوسته
داخل متن `$` و بک‌تیک را تفسیر می‌کند و کد پایتون خراب می‌شود.

### فهرست کارهای پایتونی این پروژه

```python
# ۱. استخراج ساختاریافتهٔ Word
import docx
d = docx.Document("TOA-23.docx")
for t in d.tables:
    for row in t.rows:
        print([c.text.strip() for c in row.cells])

# ۲. استخراج تصویر
import zipfile
zipfile.ZipFile("TOA-23.docx").read("word/media/image1.png")

# ۳. کدگذاری Base64
import base64
base64.b64encode(open("logo.png","rb").read()).decode()

# ۴. ابعاد تصویر
from PIL import Image
Image.open("logo.png").size        # (698, 258)

# ۵. بررسی سلامت
b'\x00' in open("f.html","rb").read()

# ۶. شمارش الگو
len(re.findall(rb'/Type\s*/Page[^s]', pdf_bytes))
```

---

# بخش دو — ساخت پروژه

---

## مقدمه — چه ساختیم و چرا

### مسئله

شرکت (تحکیم عمران) سه فرم کاغذی واقعی داشت:

| فایل | عنوان | کاغذ | مراحل گردش |
| --- | --- | --- | --- |
| `TOA-23.docx` | درخواست مأموریت | A5 افقی | ۴ |
| `TOA-28.docx` | فرم درخواست وام | A4 عمودی | ۴ |
| `TOA-29 - Rev2.docx` | درخواست خرید کالا | A4 افقی | ۵ |

هدف: یک دموی قابل‌کلیک که به کارفرما نشان بدهد این فرم‌ها چگونه الکترونیکی می‌شوند —
پر کردن، ارجاع، تأیید/رد، و در نهایت خروجی PDF **دقیقاً مثل کاغذ اصلی**.

### قیدهای طراحی

این قیدها همه‌چیز را تعیین کردند، پس اول آن‌ها را بفهم:

1. **یک فایل مستقل** — بدون سرور، بدون دیتابیس، بدون اینترنت. باید با دوبار کلیک باز شود و
   روی لپ‌تاپ جلسه کار کند. ریسک فنی روز دمو = صفر.
2. **راست‌چین و فارسی** — کل چیدمان RTL.
3. **خروجی چاپ باید عین اصل باشد** — نه «شبیه»، بلکه همان چیدمان، همان اندازهٔ کاغذ.
4. **دمو، نه محصول** — نباید وقت روی چیزهایی تلف شود که در نسخهٔ واقعی به‌هرحال بازنویسی می‌شوند.

### نتیجهٔ نهایی

یک فایل `forms-demo.html` حدود ۱۸۵ کیلوبایت شامل:

- سه فرم واقعی، کلمه‌به‌کلمه از فایل‌های Word
- موتور گردش کار (تأیید، رد، بازگشت برای اصلاح)
- خروجی PDF با اندازهٔ کاغذ اختصاصی هر فرم
- تقویم شمسی
- آپلود امضای الکترونیکی
- **فرم‌ساز از صفر** که فرم تازه را به همان موتور واقعی می‌سپارد
- لوگوی واقعی شرکت جاسازی‌شده به‌صورت Base64

---

## فصل ۱ — استخراج مادهٔ خام از فایل‌های Word

### چالش: نه LibreOffice، نه pandoc

اولین کاری که کردم بررسی ابزارهای موجود بود:

```bash
which libreoffice soffice pandoc python3
python3 -c "import docx"
```

نتیجه: هیچ‌کدام نبود. حتی `pip install python-docx` هم به‌خاطر سیاست PEP 668 سیستم رد شد.

**راه‌حل:** یک محیط مجازی پایتون جدا:

```bash
python3 -m venv /tmp/docxenv
/tmp/docxenv/bin/pip install python-docx --quiet
/tmp/docxenv/bin/python3 -c "import docx; print('ok')"
```

> **درس ۱:** وقتی ابزار استاندارد در دسترس نیست، venv سریع‌ترین راه‌حل است — بدون دست‌زدن به
> پایتون سیستم. اگر آن هم نبود، `.docx` در واقع یک فایل ZIP از XML است و با
> `unzip -p file.docx word/document.xml` می‌شود مستقیم خواندش.

### استخراج ساختار (نه فقط متن)

نکتهٔ مهم: `python-docx` به‌طور پیش‌فرض پاراگراف‌ها و جدول‌ها را **جدا** برمی‌گرداند، پس ترتیب
واقعی‌شان از دست می‌رود. برای حفظ ترتیب باید مستقیم روی عناصر XML پیمایش کرد:

```python
def iter_block_items(parent):
    parent_elm = parent.element.body
    for child in parent_elm.iterchildren():
        if child.tag.endswith('}p'):    yield Paragraph(child, parent)
        elif child.tag.endswith('}tbl'): yield Table(child, parent)
```

### چیزهایی که باید استخراج می‌شد

| چه چیزی | چطور | نتیجه |
| --- | --- | --- |
| متن و جدول‌ها | `iter_block_items` بالا | متن کامل هر سه فرم |
| اندازهٔ کاغذ | `sec.page_width / 914400` (EMU→اینچ) | A5 افقی، A4 عمودی، A4 افقی |
| رنگ پس‌زمینهٔ سلول | `tcPr.find('...}shd')` → `w:fill` | `FFF2CC` (زرد کم‌رنگ سربرگ) |
| فونت فارسی | `<w:rFonts w:cs="..."/>` در XML خام | **B Nazanin** |
| جهت جدول | جست‌وجوی `<w:bidiVisual/>` | راست‌چین بصری |
| لوگو | `unzip -p file.docx word/media/image1.png` | PNG با ابعاد ۶۹۸×۲۵۸ |

### دو کشف مهم

**۱. نام واقعی شرکت.** در متن `TOA-28` نوشته بود «مدیر عامل محترم شرکت **تحکیم عمران آرتین**».
تا آن لحظه فقط «راکسیت» را می‌دانستیم که در واقع نام پروژه بود، نه نام شرکت.

**۲. فونت تجاری است.** `B Nazanin` برای استفادهٔ تجاری نیازمند لایسنس است. پس **آن را جاسازی
نکردم**؛ فقط با نام در پشتهٔ فونت معرفی کردم:

```css
--f-doc: "B Nazanin", "IRANSansX", Tahoma, "Vazirmatn", sans-serif;
```

اگر روی رایانهٔ بیننده نصب باشد (که در اغلب رایانه‌های اداری ایرانی هست) خودکار استفاده می‌شود،
وگرنه جایگزین تمیز می‌نشیند. **صفر مشکل حقوقی.**

### جاسازی لوگو

لوگو باید داخل فایل باشد (چون فایل مستقل است). راه‌حل: Base64 data URI.

```bash
unzip -p "TOA-23.docx" word/media/image1.png > logo.png
python3 -c "
import base64
print(base64.b64encode(open('logo.png','rb').read()).decode())
" > logo_b64.txt   # حدود ۹۴ کیلوبایت متن
```

سپس در جاوااسکریپت:

```js
const LOGO_SRC = "data:image/png;base64,iVBORw0KGgoAAAA...";
```

> **درس ۲:** برای ساخت فایل نهایی از یک **قالب + جایگزینی** استفاده کردم، نه چسباندن دستی:
> فایل `forms-demo-template.html` با نشانه‌های `__LOGO_BASE64__` و `__FORMS_JS__`، و یک اسکریپت
> پایتون که آن‌ها را جایگزین می‌کرد. این‌طوری قالب خوانا می‌ماند و ۹۴ کیلوبایت Base64 وسط کد
> ویرایش‌شونده نمی‌افتد.

---

## فصل ۲ — معماری داده: چرا «توکن» و نه «فیلد»

### تصمیم بنیادی پروژه

نگاه اول به یک فرم اداری این است: «یک لیست فیلد است.»

**این نگاه اشتباه است.** به این جمله از `TOA-28` نگاه کن:

> احتراماً، اینجانب **[نام]** پرسنل شرکت به دلیل مشکلات مالی نیاز مبرم به دریافت مبلغ
> **[مبلغ]** ریال وام قرض‌الحسنه از صندوق وام مجموعه را دارم…

این یک «لیست فیلد» نیست. این یک **متن روان است که چند سوراخ دارد**. اگر مدل داده‌ات «لیست
فیلد» باشد، هرگز نمی‌توانی این را بازتولید کنی.

### مدل توکن

پس هر مرحله از فرم را به‌صورت یک **دنبالهٔ توکن** مدل کردم:

```js
const T   = (text)                    => ({k:'t',   text});                        // متن ثابت
const BR  = ()                        => ({k:'br'});                               // خط جدید
const F   = (key, label, type, opts)  => ({k:'f',   key, label, type, ...opts});    // جای خالی
const CH  = (key, label, options, o)  => ({k:'c',   key, label, options, ...o});    // تک‌انتخابی
const MU  = (key, label, options)     => ({k:'m',   key, label, options});          // چندانتخابی
const SIG = (key, label, opts)        => ({k:'sig', key, label, ...opts});          // امضا
const DEC = (key)                     => ({k:'dec', key});                          // نتیجهٔ تصمیم
```

حالا همان جملهٔ بالا این‌طور نوشته می‌شود:

```js
runs: [
  T('احتراماً، اینجانب '),
  F('requesterName', 'نام و نام خانوادگی', 'text', {required:true}),
  T(' پرسنل شرکت به دلیل مشکلات مالی نیاز مبرم به دریافت مبلغ '),
  F('loanAmount', 'مبلغ وام درخواستی', 'number', {unit:'ریال', required:true}),
  T(' ریال وام قرض‌الحسنه از صندوق وام مجموعه را دارم…'),
]
```

**جای خالی دقیقاً همان‌جایی است که در جمله باید باشد.** این کل رمز موفقیت پروژه است.

### ساختار کامل یک فرم

```js
{
  id: 'toa28',
  code: 'TOA-28',
  rev: '01',
  title: 'فــرم درخواست وام',
  shortTitle: 'درخواست وام',
  pageSize: '210mm 297mm',    // برای @page در چاپ
  pageLabel: 'A4 عمودی',      // برای نمایش به کاربر
  hasItemTable: false,        // فقط TOA-29 جدول اقلام دارد
  stages: [
    {
      key: 'request',
      role: 'درخواست‌کننده',
      kind: 'submit',          // submit | decision | confirm | info
      runs: [ /* توکن‌ها */ ],
      decision: {              // فقط اگر kind === 'decision'
        key: 'ceoDecision',
        options: ['موافقت می‌گردد', 'موافقت نمی‌گردد'],
        onReject: {type:'terminal'}   // یا {type:'loop', to:'request'}
      }
    },
    // …
  ]
}
```

### ساختار وضعیت اجرا (instance)

فرم = **تعریف** (ثابت). instance = **وضعیت یک اجرای مشخص** (متغیر):

```js
{
  values: {},            // key → مقدار واردشده
  stageIndex: 0,         // الان روی کدام مرحله‌ایم
  status: 'active',      // active | rejected | done
  rejectedAtStage: null,
  log: [ {t:'۱۴۰۵/۰۵/۰۷', text:'فرایند آغاز شد…'} ],
  items: [ /* فقط TOA-29 */ ],
}
```

> **درس ۳:** جدا کردن «تعریف» از «وضعیت» باعث شد فرم‌ساز از صفر تقریباً رایگان دربیاید — چون
> فقط باید یک شیء «تعریف» بسازد و بقیهٔ موتور بدون تغییر کار می‌کند.

---

## فصل ۳ — سیستم طراحی و مسئلهٔ «کاغذ داخل برنامه»

### پالت رنگ

پالت را از دنیای موضوع گرفتم، نه از قالب‌های آماده:

```css
--ground:  #FBFAF8;   /* کاغذ کالک — نه کرمِ کلیشه‌ای */
--ink:     #1C1A17;   /* مشکی گرم */
--accent:  #A63D2E;   /* قرمزِ اکسید آهن — رنگ پرایمر میلگرد و رنگ خط‌کشی کارگاه */
--slate:   #2F4858;   /* آبی-خاکستری نقشهٔ فنی */
```

خنثی‌ها عمداً کمی به سمت رنگ تأکید متمایل‌اند (گرم)، نه خاکستری خالص.

### دو تم، سه لایه

```css
:root { /* مقادیر روشن */ }
@media (prefers-color-scheme: dark) { :root { /* مقادیر تیره */ } }
:root[data-theme="dark"]  { /* برای دکمهٔ دستی تم */ }
:root[data-theme="light"] { /* برای دکمهٔ دستی تم */ }
```

هر سه لازم‌اند: مدیا-کوئری تنظیم سیستم‌عامل را می‌گیرد، و `data-theme` باید بتواند آن را در
هر دو جهت override کند.

### مسئلهٔ کلیدی: کاغذ یک جسم خارجی است

اینجا مهم‌ترین بینش بصری پروژه است، و منشأ **سه باگ جداگانه**:

> **کاغذ، بخشی از رابط برنامه نیست. کاغذ یک شیء فیزیکی است که داخل برنامه نشان داده می‌شود.**
> پس هرگز نباید با تم برنامه عوض شود. کاغذ همیشه سفید است، جوهر همیشه تیره — حتی وقتی کل
> برنامه در حالت تاریک است.

راه‌حل نهایی (بعد از دو بار وصله‌کاری ناقص) این شد که کاغذ **مجموعه رنگ مستقل خودش** را داشته باشد:

```css
.paper{
  background:#fff;
  color:#1a1a1a;
  color-scheme:light;          /* ← کلیدی: مرورگر را مجبور می‌کند فرم‌کنترل‌ها را روشن بکشد */
  /* مجموعه رنگ محلی — هرگز با تم برنامه عوض نمی‌شود */
  --p-accent:#A63D2E;  --p-accent-soft:#F2E4E0;
  --p-ok:#2E6F4E;      --p-ok-soft:#E3F0E8;
  --p-bad:#A63D2E;     --p-bad-soft:#F2E4E0;
  --p-rule-2:#CFC7BB;
}
.paper table{ color:#1a1a1a }   /* جدول رنگ را از color-scheme می‌گیرد، نه از والد */
```

و بعد **هر** استفاده از رنگ داخل کاغذ به `--p-*` تغییر کرد:

```css
/* غلط — با تم برنامه عوض می‌شود */
.doc-blank input:focus{ background:var(--accent-soft) }

/* درست — همیشه ثابت */
.doc-blank input:focus{ background:var(--p-accent-soft) }
```

### سه دستهٔ فونت

```css
--f-ui:   "IRANSansX","IRANSans","Vazirmatn",Tahoma,...;  /* رابط برنامه */
--f-doc:  "B Nazanin","IRANSansX",Tahoma,...;             /* داخل کاغذ */
--f-mono: "Consolas","Roboto Mono","Courier New",monospace; /* کدها */
```

جدا بودن `--f-doc` از `--f-ui` عمدی است: سند باید فونت اداری داشته باشد، رابط برنامه فونت مدرن.

---

## فصل ۴ — موتور رندر: یک تابع، دو خروجی

### مهم‌ترین تصمیم معماری کل پروژه

اگر فقط یک چیز از این جزوه یاد بگیری، این باشد:

> **همان تابعی که فرم را برای پر کردن می‌کشد، فرم را برای چاپ هم می‌کشد.**
> تنها فرقش یک پارامتر بولین است.

```js
function renderTokens(tokens, values, editable){
  let html = "", para = "";
  const flush = () => {
    if (para.trim() || html === "") { html += `<p class="doc-para">${para}</p>`; para = ""; }
  };

  tokens.forEach(tok => {
    if (tok.k === 'br'){ flush(); return; }
    if (tok.k === 't'){ para += esc(tok.text); return; }

    if (tok.k === 'f'){
      const v = values[tok.key];
      if (editable){
        // ورودی قابل تایپ
        para += `<span class="doc-blank"><input data-fkey="${esc(tok.key)}" value="${esc(v||"")}"></span>`;
      } else {
        // متن ثابت یا نقطه‌چین خالی
        para += v
          ? `<span class="doc-blank">${esc(v)}</span>`
          : `<span class="doc-blank empty">......................</span>`;
      }
      return;
    }
    // … بقیهٔ انواع توکن
  });

  flush();
  return html;
}
```

### چرا این‌قدر مهم است

**«آنچه می‌بینی همان است که چاپ می‌شود» به‌صورت ساختاری تضمین می‌شود، نه با تلاش.**

اگر دو تابع جدا می‌نوشتم (یکی برای نمایش، یکی برای چاپ)، هر تغییر باید در هر دو اعمال می‌شد و
دیر یا زود از هم واگرا می‌شدند. با یک تابع، **امکان واگرایی وجود ندارد**.

### الگوی `flush`

نکتهٔ ظریف: توکن‌ها یک دنبالهٔ صاف‌اند، ولی خروجی HTML باید پاراگراف‌بندی داشته باشد. `flush`
متن انباشته را در یک `<p>` می‌بندد و انبار را خالی می‌کند — هر بار که به `BR()` می‌رسیم یا در
پایان.

شرط `para.trim() || html===""` جلوی تولید پاراگراف‌های خالی را می‌گیرد، ولی اگر کل محتوا خالی
بود دست‌کم یک پاراگراف تولید می‌کند.

### چه چیزی «قابل ویرایش» است

```js
const isCurrent = inst.status === "active" && i === inst.stageIndex;
const editable  = !forPrint && isCurrent;
```

یعنی: **فقط مرحلهٔ جاری، و فقط وقتی برای چاپ نمی‌سازیم.** مراحل گذشته قفل‌اند (متن ثابت)،
مراحل آینده کم‌رنگ‌اند (`.doc-pending`).

---

## فصل ۵ — موتور گردش کار

### هستهٔ منطق

```js
function advance(form, inst){
  const st = form.stages[inst.stageIndex];

  if (st.decision){
    const picked   = inst.values[st.decision.key];
    const isReject = picked === st.decision.options[1];   // گزینهٔ دوم همیشه «رد» است

    if (isReject){
      // حالت ۱: رد و بازگشت برای اصلاح (حلقه)
      if (st.decision.onReject?.type === 'loop'){
        const target = form.stages.findIndex(s => s.key === st.decision.onReject.to);
        logEvent(inst, `«${st.role}» درخواست را رد و برای اصلاح بازگرداند.`, "tl-reject");
        delete inst.values[st.decision.key];   // ← حیاتی! (فصل ۱۰، باگ ۳)
        inst.stageIndex = target;
        return;
      }
      // حالت ۲: رد نهایی
      inst.status = "rejected";
      inst.rejectedAtStage = inst.stageIndex;
      return;
    }
  }

  // جلو رفتن
  if (inst.stageIndex >= form.stages.length - 1){
    inst.status = "done";
  } else {
    inst.stageIndex += 1;
  }
}
```

### قرارداد «گزینهٔ دوم = رد»

تصمیم گرفتم `options[1]` همیشه گزینهٔ منفی باشد. این یک **قرارداد** است، نه یک قانون طبیعت —
ولی چون در کل کد یکدست رعایت می‌شود، کد را ساده نگه می‌دارد و جای اشتباه نمی‌گذارد.

### اعتبارسنجی مرحله

```js
function stageComplete(stage, values){
  const requiredKeys = [];
  (stage.runs||[]).forEach(tok=>{
    if ((tok.k==='f' || tok.k==='sig') && tok.required) requiredKeys.push(tok.key);
    if (tok.k==='c' && tok.required) requiredKeys.push(tok.key);
  });
  if (stage.decision && !values[stage.decision.key]) return false;   // تصمیم اجباری است
  return requiredKeys.every(k => values[k] != null && String(values[k]).trim() !== "");
}
```

خروجی این تابع مستقیم دکمهٔ «ثبت و ارجاع» را فعال/غیرفعال می‌کند.

### نمایش دوگانهٔ وضعیت

کارفرما خواسته بود وضعیت هم متنی و هم گرافیکی دیده شود:

**۱. نوار مرحله‌ای (Stepper)** — نقطهٔ سبز (انجام‌شده)، نارنجی (جاری)، قرمز (رد):

```js
let cls = "";
if (inst.status === "rejected" && inst.rejectedAtStage === i)      cls = "rejected";
else if (i < inst.stageIndex || (inst.status==="done" && i<=inst.stageIndex)) cls = "done";
else if (i === inst.stageIndex && inst.status === "active")        cls = "current";
```

**۲. رویدادنگار متنی** — هر اتفاق با تاریخ ثبت می‌شود:

```js
function logEvent(inst, text, cls){
  inst.log.push({ t: todayStr(), text, cls });
}
```

---

## فصل ۶ — چاپ و خروجی PDF

### تلاش اول (اشتباه)

```css
@media print{
  body * { visibility: hidden }              /* همه‌چیز نامرئی */
  .print-target, .print-target * { visibility: visible }   /* جز کاغذ */
}
```

منطقی به‌نظر می‌رسد. **ولی غلط است** — چون `visibility:hidden` عنصر را نامرئی می‌کند اما
**فضایش را نگه می‌دارد**. نتیجه: PDF چهار صفحه‌ای که دو صفحه‌اش خالی بود.

### راه‌حل درست: ناحیهٔ چاپ مجزا

```html
<div class="shell" id="shell"></div>     <!-- رابط برنامه -->
<div id="print-root"></div>              <!-- فقط موقع چاپ پر می‌شود -->
```

```css
#print-root{ display:none }              /* در حالت عادی اصلاً وجود ندارد */

@media print{
  body > *:not(#print-root){ display:none }   /* ← display، نه visibility */
  #print-root{ display:block }
}
```

و در لحظهٔ چاپ:

```js
if (e.target.id === "printBtn"){
  document.getElementById("print-root").innerHTML = renderPrintPaper(form, inst);
  injectPageSize(form.pageSize);
  setTimeout(() => window.print(), 30);
}
```

`display:none` عنصر را کاملاً از جریان چیدمان حذف می‌کند. مسئلهٔ صفحهٔ خالی برطرف شد.

### اندازهٔ کاغذ پویا

هر فرم اندازهٔ کاغذ خودش را دارد، پس `@page` باید در لحظه تزریق شود:

```js
let pageStyleEl = null;
function injectPageSize(sizeMm){
  if (!pageStyleEl){
    pageStyleEl = document.createElement("style");
    document.head.appendChild(pageStyleEl);
  }
  pageStyleEl.textContent = `@page{ size:${sizeMm}; margin:10mm; }`;
}
```

### تنظیمات ریز چاپ

```css
@media print{
  .doc-stage{ break-inside:avoid; page-break-inside:avoid }  /* یک مرحله نصف نشود */
  .doc-blank:not(.empty){ border-bottom:none }               /* زیر مقدار پرشده خط نباشد */
  .doc-decision-buttons{ display:none }                      /* دکمه روی کاغذ بی‌معناست */
  .stage-badge{ display:none }                               /* نشان وضعیت فقط برای صفحه */
  .doc-stage-role{ display:none }                            /* عنوان نقش تکراری */
  .doc-pending{ opacity:1; filter:none }                     /* مراحل آینده کم‌رنگ نباشند */
}
```

### محدودیتی که نمی‌شود در کد رفع کرد

سربرگ/پابرگ مرورگر (تاریخ، آدرس فایل، شمارهٔ صفحه) را **مرورگر** اضافه می‌کند، نه صفحهٔ ما.
کاربر باید در پنجرهٔ چاپ، تیک **«Headers and footers»** را بردارد.

> **درس ۴:** بعضی چیزها واقعاً خارج از کنترل کدند. به‌جای تلاش برای دور زدنشان، صادقانه به
> کاربر بگو چه کار کند.

---

## فصل ۷ — تقویم شمسی

### الگوریتم

از الگوریتم استاندارد Borkowski (همان که `jalaali-js` استفاده می‌کند) استفاده کردم:

```js
function toJalali(gy, gm, gd){
  const g_d_m = [0,31,59,90,120,151,181,212,243,273,304,334];
  const gy2 = (gm > 2) ? (gy + 1) : gy;
  let days = 355666 + (365*gy) + Math.floor((gy2+3)/4) - Math.floor((gy2+99)/100) +
             Math.floor((gy2+399)/400) + gd + g_d_m[gm-1];
  let jy = -1595 + (33 * Math.floor(days/12053));
  days %= 12053;
  jy += 4 * Math.floor(days/1461);
  days %= 1461;
  if (days > 365){ jy += Math.floor((days-1)/365); days = (days-1) % 365; }
  let jm, jd;
  if (days < 186){ jm = 1 + Math.floor(days/31);        jd = 1 + (days % 31); }
  else           { jm = 7 + Math.floor((days-186)/30);  jd = 1 + ((days-186) % 30); }
  return [jy, jm, jd];
}
```

### چرا فقط یک‌طرفه

فقط میلادی→شمسی پیاده شد، نه برعکس. **چون لازم نبود.** هیچ‌جای برنامه محاسبهٔ تقویمی انجام
نمی‌دهد؛ تاریخ فقط ذخیره و نمایش داده می‌شود.

> **درس ۵:** کد ننوشته، کدی است که باگ ندارد. تبدیل معکوس را وقتی بنویس که واقعاً لازم شد.

### راستی‌آزمایی

قبل از اعتماد، با تاریخ‌های مرجع شناخته‌شده تست کردم:

```
2024-03-20 → [1403, 1, 1]  ✓ (اول فروردین ۱۴۰۳)
2025-03-21 → [1404, 1, 1]  ✓
2000-01-01 → [1378, 10, 11] ✓
```

### ورودی تاریخ: سه کشو به‌جای date picker

`<input type="date">` بومی مرورگر تقویم میلادی نشان می‌دهد. راه‌حل: سه `<select>`:

```js
<select data-role="jdate" data-part="d" data-key="..."> روز </select>
<select data-role="jdate" data-part="m" data-key="..."> ماه (فروردین…اسفند) </select>
<select data-role="jdate" data-part="y" data-key="..."> سال (±۳ سال از امروز) </select>
```

و هنگام تغییر، هر سه با هم خوانده می‌شوند:

```js
if (t.dataset.role === "jdate"){
  const group = document.querySelectorAll(`[data-role="jdate"][data-key="${CSS.escape(key)}"]`);
  const vals = {};
  group.forEach(s => { vals[s.dataset.part] = s.value; });
  if (vals.d && vals.m && vals.y) inst.values[key] = jalaliDateStr(+vals.y, +vals.m, +vals.d);
  else delete inst.values[key];   // ناقص = خالی
}
```

---

## فصل ۸ — امضای الکترونیکی

### از متن به تصویر

نسخهٔ اول: یک `<input type="text">` که اسم را می‌گرفت. کارفرما گفت باید تصویر امضا آپلود شود.

```js
if (tok.k === 'sig'){
  const v = values[tok.key];   // ← data URI تصویر
  if (editable){
    const preview = v
      ? `<img src="${v}" class="sig-thumb">
         <button class="btn ghost sig-remove" data-sigkey="${esc(tok.key)}">حذف</button>`
      : `<button class="btn sig-upload" data-sigkey="${esc(tok.key)}">آپلود امضای الکترونیکی</button>`;
    html += `<div class="doc-para sig-row">${esc(tok.label)}:
      <input type="file" accept="image/*" data-sigfile="${esc(tok.key)}" class="sig-file-input">
      ${preview}</div>`;
  } else if (v){
    html += `<div class="doc-signature"><img src="${v}" class="sig-thumb sig-thumb-print">
      <span>ثبت‌شده در ${esc(values[tok.key+"__ts"] || todayStr())}</span></div>`;
  }
}
```

### خواندن فایل

```js
if (t.dataset.sigfile){
  const file = t.files?.[0];
  if (!file) return;
  const reader = new FileReader();
  reader.onload = () => {
    inst.values[key] = reader.result;              // data:image/png;base64,...
    inst.values[key + "__ts"] = todayStr();        // مهر زمانی
    saveStore(); render();
  };
  reader.readAsDataURL(file);
}
```

چون امضا به‌صورت data URI ذخیره می‌شود، **در PDF هم بدون هیچ کار اضافه‌ای می‌آید** — تصویر
جزئی از HTML است.

### الگوی ورودی فایل پنهان

`<input type="file">` بومی زشت است. الگوی رایج:

```css
.sig-file-input{ position:absolute; opacity:0; width:0; height:0; pointer-events:none }
```

```js
const input = sigUpload.parentElement.querySelector(`[data-sigfile="${CSS.escape(key)}"]`);
if (input) input.click();   // دکمهٔ زیبا، ورودی پنهان را کلیک می‌کند
```

---

## فصل ۹ — فرم‌ساز از صفر: دو نسل

این بخش دو بار نوشته شد. **نسل اول شکست خورد** و فهمیدن دلیلش مهم‌ترین درس این فصل است.

### نسل اول: لیست فیلد (شکست‌خورده)

مدل داده:

```js
stages: [
  { role:'', kind:'submit', fields:[ {label:'نام', type:'text', required:true} ] }
]
```

رابط کاربری: یک ردیف «نام فیلد + نوع + دکمهٔ افزودن». هر فیلد در یک خط جدا می‌نشست:

```
نام کارمند: ......................
مبلغ: ......................
```

**بازخورد کاربر:** «خیلی خوب نمیشه متن بلند نوشت، نمیشه نقطه‌چین و جای خالی جاگذاری کرد،
سخته کار کردن باهاش.»

**تشخیص:** مدل داده اشتباه بود. با «لیست فیلد» هرگز نمی‌شود جای خالی را **وسط جمله** گذاشت.
دقیقاً همان چیزی که در فصل ۲ فهمیده بودیم — ولی در فرم‌ساز فراموشش کرده بودم.

> **درس ۶:** اگر رابط کاربری سخت است، معمولاً مشکل از رابط نیست — از **مدل داده** است.
> وصله زدن به رابط، مشکل مدل داده را حل نمی‌کند.

### نسل دوم: متن روایی با نشانه‌گذاری

مدل داده جدید — هر مرحله فقط **یک رشتهٔ متن**:

```js
stages: [ { role:'', kind:'submit', text: '' } ]
```

و جای خالی با نشانه‌گذاری داخل خود متن:

```
احتراماً، اینجانب [[نام کارمند*]] درخواست [[مبلغ|number]] ریال دارم.
```

### دستور زبان نشانه‌گذاری

| الگو | معنی |
| --- | --- |
| `[[برچسب]]` | جای خالی متنی |
| `[[برچسب*]]` | اجباری (ستاره) |
| `[[برچسب\|number]]` | عدد |
| `[[برچسب\|date]]` | تاریخ شمسی |
| `[[برچسب\|textarea]]` | متن بلند |
| `[[برچسب\|choice:الف,ب,ج]]` | انتخابی با گزینه‌ها |
| `[[برچسب\|signature]]` | امضا |
| `[[برچسب\|decision:تأیید,رد]]` | تصمیم |

### تجزیه‌گر

```js
const SCRATCH_MARKER_RE = /\[\[([^\]|*]+?)(\*)?(?:\|([a-zA-Z]+)(?::([^\]]+))?)?\]\]/g;

function parseScratchText(text, stageIdx){
  const runs = [];
  let decisionMeta = null;
  const pushText = (chunk) => {
    chunk.split("\n").forEach((line, i) => {
      if (i > 0) runs.push(BR());
      if (line)  runs.push(T(line));
    });
  };

  let last = 0, m, fi = 0;
  SCRATCH_MARKER_RE.lastIndex = 0;                    // ← ریست حیاتی برای regex سراسری
  while ((m = SCRATCH_MARKER_RE.exec(text))){
    if (m.index > last) pushText(text.slice(last, m.index));   // متن قبل از نشانه
    const label = m[1].trim(), required = !!m[2],
          type = (m[3]||'text').toLowerCase(), extra = m[4];
    const key = "s" + stageIdx + "f" + (++fi);

    if (type === 'choice')         runs.push(CH(key, label, extra.split(","), {required}));
    else if (type === 'signature') runs.push(SIG(key, label, {required}));
    else if (type === 'decision'){
      decisionMeta = { label, options: extra.split(",") };
      runs.push({__decisionPlaceholder:true});         // جای‌نگه‌دار
    }
    else runs.push(F(key, label, type, {required}));

    last = SCRATCH_MARKER_RE.lastIndex;
  }
  if (last < text.length) pushText(text.slice(last));  // متن باقی‌مانده
  return { runs, decisionMeta };
}
```

**الگوی جای‌نگه‌دار:** توکن `DEC` به کلید تصمیم مرحله نیاز دارد، ولی آن کلید در زمان تجزیه هنوز
معلوم نیست. پس یک شیء نشانه‌دار می‌گذارم و بعداً تعویض می‌کنم:

```js
stage.runs = runs.map(r => r.__decisionPlaceholder ? DEC(decKey) : r);
```

### چرا این طراحی برنده شد

خروجی `parseScratchText` **دقیقاً همان ساختاری است که سه فرم دستی دارند**. یعنی:

```js
function buildFormFromDraft(){
  const stages = d.stages.map((st, si) => {
    const { runs, decisionMeta } = parseScratchText(st.text, si);
    return { key:"s"+si, role:st.role, kind:st.kind, runs, /* … */ };
  });
  return { id, code:d.code, title:d.title, pageSize, stages };
}
```

و بعد این شیء به همان موتور اجرا سپرده می‌شود. **هیچ کد اجرایی جدیدی لازم نشد.**

پیش‌نمایش هم از همان `renderTokens` واقعی استفاده می‌کند:

```js
const bodyHtml = renderTokens(inlineRuns, {}, false);   // همان تابع، حالت قفل
```

پس آنچه در پیش‌نمایش می‌بینی، تضمیناً همان چیزی است که در اجرا خواهی دید.

### نوار ابزار: کاربر نباید نشانه‌گذاری را حفظ کند

دستور زبان بالا برای کاربر نهایی سخت است. پس دکمه‌هایی گذاشتم که آن را **در محل مکان‌نما** درج
می‌کنند:

```js
function insertScratchSnippet(si, snippet){
  const ta = document.querySelector(`.sc-narrative[data-si="${si}"]`);
  const start = ta.selectionStart, end = ta.selectionEnd, val = ta.value;
  const before = val.slice(0, start), after = val.slice(end);
  // فاصله‌گذاری هوشمند: به کلمهٔ قبل/بعد نچسبد
  const leadSpace  = before && !/[\s\n]$/.test(before) ? " " : "";
  const trailSpace = after  && !/^[\s\n]/.test(after)  ? " " : "";
  const insert = leadSpace + snippet + trailSpace;
  ta.value = before + insert + after;
  scratchDraft.stages[si].text = ta.value;
  const pos = start + insert.length;
  ta.focus(); ta.setSelectionRange(pos, pos);   // مکان‌نما بعد از درج
  updateScratchPreview();
}
```

و در نهایت یک ردیف **«فیلدهای آماده»** با ۱۱ فیلد پرکاربرد از خود همان سه فرم واقعی — که با یک
کلیک و **بدون هیچ سؤالی** درج می‌شوند، چون نام و نوعشان از قبل معلوم است:

```js
const SCRATCH_PRESETS = [
  {label:'نام و نام خانوادگی', type:'text', required:true},
  {label:'تاریخ', type:'date'},
  {label:'مبلغ (ریال)', type:'number'},
  {label:'نحوهٔ پرداخت', type:'choice', options:['نقدی','واریز بانکی','سایر']},
  {label:'امضا', type:'signature', required:true},
  // …
];
```

### لوگوی قابل‌تعویض

سه فرم نمونه لوگوی ثابت شرکت را دارند (چون بازتولید سند واقعی‌اند)، ولی فرم‌های تازه‌ساخته باید
بتوانند لوگوی خودشان را داشته باشند — مثلاً برای شرکت دیگر یا یک واحد خاص.

پیاده‌سازی در سه نقطه:

```js
// ۱. پیش‌نویس یک فیلد لوگو دارد؛ null یعنی «از پیش‌فرض استفاده کن»
let scratchDraft = { title:"", code:"NEW-01", pageKey:"a4p", logo:null, stages:[…] };

// ۲. هرجا لوگو کشیده می‌شود، اول لوگوی فرم، بعد پیش‌فرض
<img src="${form.logo || LOGO_SRC}">     // در هدر سند
<img src="${d.logo || LOGO_SRC}">        // در پیش‌نمایش فرم‌ساز

// ۳. فرم ساخته‌شده لوگو را با خودش می‌برد
return { id, code, title, pageSize, logo: d.logo || null, stages };
```

الگوی `form.logo || LOGO_SRC` یعنی **هیچ فرم موجودی نیاز به تغییر ندارد** — چون `logo` نداشته
باشند، خودکار پیش‌فرض را می‌گیرند.

### کوچک‌سازی خودکار تصویر — چرا لازم بود

کل وضعیت برنامه در `localStorage` ذخیره می‌شود، و سهمیهٔ آن حدود ۵ تا ۱۰ مگابایت است. اگر کاربر
یک عکس ۴ مگابایتی به‌عنوان لوگو آپلود کند، Base64 آن حدود ۵.۳ مگابایت می‌شود و ذخیره‌سازی
**بی‌صدا شکست می‌خورد** (چون `saveStore` داخل `try/catch` است).

راه‌حل: قبل از ذخیره، با `canvas` کوچکش کن:

```js
const LOGO_MAX_WIDTH = 600;
function readLogoFile(file, cb){
  const reader = new FileReader();
  reader.onload = () => {
    const img = new Image();
    img.onload = () => {
      if (img.width <= LOGO_MAX_WIDTH){ cb(reader.result); return; }   // نیازی نیست
      const scale  = LOGO_MAX_WIDTH / img.width;
      const canvas = document.createElement("canvas");
      canvas.width  = LOGO_MAX_WIDTH;
      canvas.height = Math.round(img.height * scale);   // حفظ نسبت ابعاد
      canvas.getContext("2d").drawImage(img, 0, 0, canvas.width, canvas.height);
      cb(canvas.toDataURL("image/png"));
    };
    img.onerror = () => cb(reader.result);   // اگر تصویر خوانده نشد، خام را بده
    img.src = reader.result;
  };
  reader.readAsDataURL(file);
}
```

**تکنیک کلیدی:** `canvas` می‌تواند هر تصویری را در ابعاد دلخواه بکشد و بعد با `toDataURL` دوباره
به data URI تبدیل کند. این استاندارد‌ترین راه تغییر اندازهٔ تصویر در مرورگر است.

نتیجهٔ تست واقعی: تصویر ۱۲۰۰×۴۰۰ آپلود شد → ۶۰۰×۲۰۰ ذخیره شد، حجم data URI حدود ۱۲ کیلوبایت.

### دو ریزه‌کاری که فراموش می‌شوند

```js
if (t.id === "sc-logo-file"){
  const file = t.files && t.files[0];
  t.value = "";                       // ← ۱. ریست ورودی فایل
  if (!file) return;
  if (!file.type.startsWith("image/")){ alert("لطفاً یک فایل تصویری انتخاب کنید."); return; }  // ۲
  readLogoFile(file, dataUrl => { scratchDraft.logo = dataUrl; render(); });
}
```

۱. **`t.value = ""`** — بدون این، اگر کاربر همان فایل را دوباره انتخاب کند، رویداد `change`
   شلیک **نمی‌شود** (چون مقدار عوض نشده). این باگ کلاسیک ورودی فایل است.

۲. **بررسی نوع فایل** — صفت `accept="image/*"` فقط پیشنهاد است؛ کاربر می‌تواند در پنجرهٔ فایل
   «همهٔ فایل‌ها» را انتخاب کند. بررسی واقعی باید در کد باشد.

---

## فصل ۱۰ — کالبدشکافی باگ‌ها (مهم‌ترین فصل)

این فصل ارزشمندترین بخش جزوه است. هر باگ با ساختار یکسان: **علامت → علت ریشه‌ای → درس → اصلاح**.

---

### باگ ۱ — بایت خالی (NUL) داخل فایل

**علامت:** بعد از یک ویرایش، `grep` روی فایل هیچ نتیجه‌ای برنمی‌گرداند. انگار فایل خالی است.

```bash
$ grep -c "function" forms-demo.html
# هیچ خروجی، exit code 1
```

**تشخیص:**

```bash
$ grep -ac "function" forms-demo.html   # با -a (binary as text)
30
```

پس فایل **دودویی تشخیص داده می‌شد**. با پایتون دنبال علت گشتم:

```python
data = open(path,'rb').read()
idx = data.find(b'\x00')
print('NUL at byte', idx)
print(repr(data[idx-80:idx+80]))
```

**علت ریشه‌ای:** در یک عبارت باقاعده نوشته بودم `[\x00-\x1F]` تا کاراکترهای کنترلی را از نام
پوشه حذف کنم. ولی در مسیر انتقال متن، `\x00` به **بایت واقعی صفر** تبدیل شده بود، نه دنبالهٔ
فراری چهارحرفی.

**اصلاح:** مستقیم روی بایت‌ها، نه از طریق ابزار متنی:

```python
new_block = (
    b'function sanitizePath(seg){\n'
    b'  return String(seg).replace(/[\\\\/:*?"<>|]/g, "-")...'
)
assert old_block.count(b'\x00') == 1
data2 = data[:start] + new_block + data[end:]
assert b'\x00' not in data2
open(path,'wb').write(data2)
```

> **درس ۷:** هر بار که فایل را ویرایش می‌کنی، سلامت پایه‌اش را چک کن:
> ```bash
> python3 -c "print('NUL:', b'\x00' in open('f.html','rb').read())"
> ```
> این چک را بعد از **هر** ویرایش انجام دادم؛ ارزان است و از فاجعه جلوگیری می‌کند.

---

### باگ ۲ — متن سربرگ جدول در حالت تیره نامرئی می‌شد

**علامت:** «این نام‌ها توی هدر سفید شدن و اصلاً پیدا نیستن.»

**بازتولید با اتوماسیون** (قبل از هر اصلاحی):

```js
const page = await browser.newPage({ colorScheme: 'dark' });
const info = await page.evaluate(() => {
  const th = document.querySelector('table.item-tbl th');
  const cs = getComputedStyle(th);
  return { color: cs.color, background: cs.backgroundColor };
});
// { color: 'rgb(239, 234, 227)', background: 'rgb(238, 238, 238)' }
```

متن روشن روی پس‌زمینهٔ روشن. تأیید شد.

**تشخیص عمیق‌تر** — پیمایش زنجیرهٔ ارث‌بری:

```js
let el = th;
while (el && el.nodeType === 1) {
  out.push({ tag: el.tagName, cls: el.className, color: getComputedStyle(el).color });
  el = el.parentElement;
}
```

نتیجه: `DIV.paper` رنگ `rgb(26,26,26)` داشت، ولی `TABLE` داخلش `rgb(239,234,227)`.

**اصلاح اول (که کار کرد ولی علامت را درمان می‌کرد):**

```css
.paper{ color-scheme: light }    /* مرورگر را قفل می‌کند */
.paper table{ color:#1a1a1a }    /* صریح، برای اطمینان */
```

مشکل حل شد و تست‌ها سبز شدند. **ولی علت ریشه‌ای هنوز پیدا نشده بود.**

---

#### ادامهٔ ماجرا: علت واقعی، هفته‌ها بعد

موقع نوشتن همین جزوه، داشتم ساختار HTML فایل را بررسی می‌کردم که این را دیدم:

```js
document.compatMode   // "BackCompat"
document.doctype      // null
```

فایل **DOCTYPE نداشت** و مرورگر در **حالت Quirks** بود. و یکی از تفاوت‌های شناخته‌شدهٔ حالت
Quirks این است که **جدول‌ها ویژگی‌های متن را از والدشان ارث نمی‌برند**.

برای اثبات، دو نسخهٔ یکسان ساختم — هر دو بدون اصلاح قبلی — یکی با DOCTYPE و یکی بدون:

```js
for (const f of ['test-quirks','test-standards']){
  const page = await (await browser.newContext({colorScheme:'dark'})).newPage();
  await page.goto('file:///tmp/'+f+'.html');
  const r = await page.evaluate(() => ({
    mode: document.compatMode,
    paperColor: getComputedStyle(document.querySelector('.paper')).color,
    thColor:    getComputedStyle(document.querySelector('table.item-tbl th')).color,
  }));
  console.log(f, r);
}
```

خروجی:

```
test-quirks    { mode:'BackCompat',  paperColor:'rgb(26,26,26)', thColor:'rgb(239,234,227)' }
test-standards { mode:'CSS1Compat',  paperColor:'rgb(26,26,26)', thColor:'rgb(26,26,26)'   }
```

**تأیید قطعی.** در حالت استاندارد، جدول رنگ را درست ارث می‌برد و باگ اصلاً وجود ندارد.

**اصلاح ریشه‌ای:**

```html
<!DOCTYPE html>
<title>دموی فرم‌ساز و گردش کار — تحکیم عمران</title>
```

**ولی این تغییر بی‌عارضه نبود.** حالت رندر عوض شد و ابعاد کمی جابه‌جا شدند:

```
قبل (Quirks)    → tableH: 89،  paperH: 1162، PDF: ۱ صفحه
بعد (Standards) → tableH: 113، paperH: 1188، PDF: ۲ صفحه
```

جدول در حالت استاندارد **درست‌تر** رندر می‌شد (ارتفاع سلول‌ها واقعی)، ولی همین باعث سرریز به
صفحهٔ دوم شد. با کمی فشرده‌سازی مخصوص چاپ حل شد:

```css
@media print{
  table.item-tbl th, table.item-tbl td{ padding:3px 5px; font-size:10px }
  table.item-tbl th .auto-tag{ display:none }
  .item-tbl-wrap{ margin:6px 0 8px }
}
```

نتیجهٔ نهایی: `TOA-28 → ۱ صفحه`، `TOA-29 → ۱ صفحه`، `TOA-23 → ۲ صفحه` (که از قبل هم همین بود،
چون A5 کوچک است). و کل مجموعهٔ تست‌ها بدون خطا.

> **درس ۸:** «کار می‌کند» با «درست است» فرق دارد. اصلاح اول علامت را برطرف کرد؛ اصلاح دوم
> علت را. اگر فقط علامت را درمان کنی، همان علت جای دیگری دوباره سر بلند می‌کند — و در حالت
> Quirks، ده‌ها جای دیگر برای سر بلند کردن وجود دارد.
>
> **درس ۹:** وقتی حالت رندر را عوض می‌کنی، **همه‌چیز** را دوباره تست کن — نه فقط چیزی که
> می‌خواستی درست کنی. من هر پنج مجموعهٔ تست و شمارش صفحات هر سه PDF را دوباره گرفتم.

---

### باگ ۳ — تصمیم رد‌شده پاک نمی‌شد

**علامت:** در `TOA-29`، وقتی «دفتر فنی» درخواست را رد و برای اصلاح برمی‌گرداند، بعد از ارسال
مجدد، تصمیم رد قبلی **هنوز ثبت‌شده** بود.

**تأثیر واقعی:** دکمهٔ «ثبت و ارجاع» فوراً فعال بود و کاربر می‌توانست بدون بازبینی واقعی
دوباره رد کند — یا بدتر، فرایند خودکار جلو برود.

**علت ریشه‌ای:** در شاخهٔ حلقه، فقط `stageIndex` عوض می‌شد ولی مقدار تصمیم در `values` می‌ماند.

**اصلاح — یک خط:**

```js
if (st.decision.onReject?.type === 'loop'){
  const target = form.stages.findIndex(s => s.key === st.decision.onReject.to);
  logEvent(inst, `«${st.role}» درخواست را رد و برای اصلاح بازگرداند.`, "tl-reject");
  delete inst.values[st.decision.key];    // ← این خط
  inst.stageIndex = target;
  return;
}
```

**راستی‌آزمایی:**

```js
const pickedOnArrival = await page.locator('.dec-btn.picked-ok, .dec-btn.picked-no').count();
console.log('pre-picked buttons:', pickedOnArrival);   // 0 ✓
```

> **درس ۱۰:** وقتی وضعیت را به عقب برمی‌گردانی، باید **همهٔ** آثار جانبی را هم برگردانی.
> «برگشت به مرحلهٔ قبل» یعنی وضعیت آن مرحله باید مثل بار اول باشد.

---

### باگ ۴ — صفحات خالی در PDF

**علامت:** کاربر PDF چهار صفحه‌ای فرستاد که دو صفحه‌اش تقریباً خالی بود.

**بازتولید با PDF واقعی** (نه فقط پیش‌نمایش):

```js
await page.pdf({ path: 'test.pdf', printBackground: true, preferCSSPageSize: true });
```

```python
import re
data = open('test.pdf','rb').read()
print('pages:', len(re.findall(rb'/Type\s*/Page[^s]', data)))   # 3
```

**علت ریشه‌ای:** `visibility:hidden` عنصر را نامرئی می‌کند اما **فضایش را حفظ می‌کند**. کل رابط
برنامه (سربرگ، تب‌ها، فوتر) هنوز ارتفاع اشغال می‌کرد.

**اصلاح:** معماری چاپ عوض شد — ناحیهٔ مجزا با `display:none` (فصل ۶).

**نتیجه:** ۳ صفحه → ۲ صفحه، بدون صفحهٔ خالی.

> **درس ۱۱:** `visibility:hidden` ≠ `display:none`. اولی فضا نگه می‌دارد، دومی نه. در چاپ،
> تفاوتشان یعنی صفحهٔ خالی.

---

### باگ ۵ — از دست رفتن فوکوس در جدول اقلام (جدی‌ترین)

**علامت:** «نه تنها بیشتر از یه حرف رو نمیتونه پر کنه، بلکه به‌محض اینکه چیزی می‌نویسی خودکار
میره یه ردیف جدید درست می‌کنه.»

**علت ریشه‌ای:**

```js
// کد معیوب
if (t.dataset.irow != null){
  inst.items[r][c] = t.value;
  const wrap = document.querySelector(".item-tbl-wrap");
  wrap.outerHTML = renderItemTable(inst, true);   // ← کل جدول از نو ساخته می‌شود
}
```

با **هر حرف تایپ‌شده** کل جدول بازسازی می‌شد. عنصر `<input>` که کاربر داخلش تایپ می‌کرد از DOM
حذف و یک عنصر جدید جایش می‌نشست → فوکوس می‌پرید → حرف بعدی جای دیگری می‌رفت.

توضیح رفتار عجیب «ساخت ردیف جدید»: فوکوس گم‌شده باعث می‌شد کلیدها به عناصر دیگر برسند.

**اصلاح:** فقط سلول‌های محاسباتی را به‌روز کن، به ورودی‌ها دست نزن:

```js
if (t.dataset.irow != null){
  inst.items[r][c] = t.value;
  saveStore();
  updateItemTotals(inst);      // ← فقط اعداد جمع
  return;
}

function updateItemTotals(inst){
  let sum = 0;
  (inst.items||[]).forEach((row, ri) => {
    const tot = itemTotal(row); sum += tot;
    const cell = document.querySelector(`[data-role="rowtotal"][data-row="${ri}"]`);
    if (cell) cell.textContent = tot ? fa(tot.toLocaleString("en-US")) : "—";
  });
  const grand = document.querySelector('[data-role="grandtotal"]');
  if (grand) grand.textContent = fa(sum.toLocaleString("en-US"));
}
```

**راستی‌آزمایی با تایپ واقعی:**

```js
await nameField.type('سیمان تیپ دو پرتلند', {delay: 30});   // نه fill()
console.log('value:', await nameField.inputValue());        // رشتهٔ کامل ✓
console.log('focused:', await nameField.evaluate(el => el === document.activeElement)); // true ✓
```

> **درس ۱۲ (طلایی):** **هرگز عنصری را که کاربر در حال تایپ داخلش است بازسازی نکن.**
> در رویداد `input`، فقط چیزهایی را به‌روز کن که کاربر داخلشان نیست.
> برای تست، از `.type()` استفاده کن نه `.fill()` — چون `fill()` مقدار را یکجا می‌گذارد و این
> دسته باگ‌ها را نشان نمی‌دهد.

همین باگ در فرم‌ساز از صفر هم پیش آمد (تایپ در عنوان فرم) و با همان اصل حل شد: تایپ متن فقط
**پیش‌نمایش** را به‌روز می‌کند، نه پنلی که کاربر در آن تایپ می‌کند.

---

### باگ ۶ — اعداد فارسی محاسبه نمی‌شدند

**علامت:** «فقط در صورتی که عدد انگلیسی باشه اون مبلغ کل کار می‌کنه.»

**علت ریشه‌ای:**

```js
const q = parseFloat(row.qty) || 0;    // parseFloat("۱۰") → NaN → 0
```

`parseFloat` فقط ارقام ASCII را می‌شناسد.

**اصلاح:**

```js
const faToEn = s => String(s).replace(/[۰-۹]/g, d => "۰۱۲۳۴۵۶۷۸۹".indexOf(d));

function itemTotal(row){
  const q = parseFloat(faToEn(row.qty||"")) || 0;
  const p = parseFloat(faToEn(row.price||"")) || 0;
  return q * p;
}
```

**نکتهٔ مهم:** بعد از پیدا کردن این باگ، دنبال **همان الگو در جاهای دیگر** گشتم و در تابع
`money()` هم پیدایش کردم — جایی که هنوز کسی گزارشش نداده بود:

```js
function money(v){
  const n = faToEn(v).replace(/[^\d]/g,"");   // قبلاً: String(v).replace(...)
  return n ? fa(Number(n).toLocaleString("en-US")) : "";
}
```

> **درس ۱۳:** وقتی یک باگ پیدا کردی، بپرس «این الگو کجای دیگری هست؟» معمولاً یکی نیست.

---

### باگ ۷ — ترتیب قواعد CSS

**علامت:** استایل بزرگ‌کردن فونت پیش‌نمایش اصلاً اعمال نمی‌شد.

**تشخیص:**

```js
const titleFontSize = await page.evaluate(() =>
  getComputedStyle(document.querySelector('.scratch-preview-card .dh-title')).fontSize);
console.log(titleFontSize);   // "15px" — انتظار: "20px"
```

**علت ریشه‌ای:** هر دو قاعده **ویژگی یکسان** داشتند (هر دو دو کلاس)، پس **ترتیب در فایل**
تعیین‌کننده بود:

```css
/* خط ۱۰۶ */ .scratch-preview-card .dh-title{ font-size:19px }   /* زودتر */
/* خط ۲۲۵ */ .doc-header .dh-title{ font-size:15px }             /* دیرتر → برنده */
```

**اصلاح:** قاعده را بعد از قاعدهٔ پایه منتقل کردم.

> **درس ۱۴:** وقتی دو انتخابگر ویژگی برابر دارند، **آخری برنده است**. اگر استایلی اعمال نمی‌شود،
> اول ویژگی و ترتیب را چک کن — نه اینکه `!important` بزنی.

---

### دو مورد که **باگ نبودند** (به همان اندازه آموزنده)

**۱. متن برعکس در PDF.** کاربر دید که در متن استخراج‌شده از PDF نوشته `-23TOA` به‌جای `TOA-23`.
بررسی کردم: در **تصویر واقعی صفحه** کاملاً درست بود. مشکل از ابزار **استخراج متن** از PDF بود
(که با متن دوجهته RTL/LTR بد رفتار می‌کند)، نه از سند.

> **درس ۱۵:** قبل از رفع «باگ»، تأیید کن که واقعاً باگ است. من نزدیک بود چیزی را که سالم بود
> «درست» کنم.

**۲. «مبلغ کل تخمینی پر نمی‌شود».** با تست مستقیم بررسی کردم:

```
price input count found: 1
price input value after typing: 1200000       ← سالم
computed row total: ۱۲,۰۰۰,۰۰۰               ← سالم
```

ستون «قیمت واحد» کاملاً کار می‌کرد. چیزی که پر نمی‌شد ستون «مبلغ کل» بود که **عمداً خودکار
محاسبه می‌شود** و اصلاً ورودی ندارد — ولی از نظر بصری شبیه فیلد خالی به‌نظر می‌رسید.

**اصلاح ≠ کد، اصلاح = وضوح:**

```css
table.item-tbl td.auto-cell{ background:#f1efe9; color:#666; font-style:italic; cursor:not-allowed }
table.item-tbl th .auto-tag{ font-size:9px; font-style:italic; color:#888 }
```

```html
<th>مبلغ کل تخمینی<span class="auto-tag">خودکار</span></th>
```

> **درس ۱۶:** بعضی «باگ»ها مسئلهٔ طراحی‌اند نه مسئلهٔ کد. اگر چیزی شبیه ورودی به‌نظر می‌رسد،
> کاربر تلاش می‌کند در آن تایپ کند.

---

## فصل ۱۱ — روش تست

### سه لایه بررسی

**لایهٔ ۱ — سلامت فایل (بعد از هر ویرایش):**

```bash
python3 -c "print('NUL:', b'\x00' in open('forms-demo.html','rb').read())"
```

**لایهٔ ۲ — صحت نحوی جاوااسکریپت:**

```bash
python3 - <<'PY'
import re
html = open("forms-demo.html", encoding="utf-8").read()
m = re.search(r"<script>(.*)</script>", html, re.S)
open("/tmp/check.js","w",encoding="utf-8").write(m.group(1))
PY
node --check /tmp/check.js && echo "SYNTAX OK"
```

**لایهٔ ۳ — رفتار واقعی در مرورگر:**

```js
import { chromium } from './node_modules/playwright/index.mjs';
const browser = await chromium.launch();
const page = await browser.newPage({ viewport:{width:1400,height:1100} });

const errors = [];
page.on('pageerror', e => errors.push('PAGEERROR: ' + e.message));
page.on('console', m => { if (m.type()==='error') errors.push('CONSOLE: ' + m.text()); });

await page.goto('file:///path/to/forms-demo.html');
// … تعامل …
console.log('ERRORS:', errors);   // باید [] باشد
```

### تست ساختاری داده

ساختار `FORMS` را جدا اجرا کردم تا از سلامتش مطمئن شوم:

```js
console.log('forms:', FORMS.length);
const ids = new Set();
FORMS.forEach(f => {
  if (ids.has(f.id)) console.log('DUP ID', f.id);
  ids.add(f.id);
  f.stages.forEach(st => {
    if (st.decision?.onReject?.type === 'loop'){
      const target = f.stages.find(s => s.key === st.decision.onReject.to);
      if (!target) console.log('LOOP TARGET NOT FOUND', st.key);
    }
  });
});
```

خروجی واقعی:

```
forms: 3
  TOA-23 | stages: 4 | page: 210mm 148mm
  TOA-28 | stages: 4 | page: 210mm 297mm
  TOA-29 | stages: 5 | page: 297mm 210mm
total stages: 13 | total tokens: 154 | keyed tokens: 40
```

یعنی سه فرم، ۱۳ مرحله، ۱۵۴ توکن که ۴۰تایشان جای خالی/انتخاب/امضا هستند — بدون شناسهٔ تکراری و
بدون مقصد حلقهٔ گم‌شده.

### تست تجزیه‌گر به‌صورت مجزا

برای فرم‌ساز از صفر، تجزیه‌گر را از فایل بیرون کشیدم و مستقل تست کردم:

```bash
python3 -c "استخراج بخش parseScratchText" > /tmp/base.js
cat /tmp/base.js /tmp/test.js > /tmp/full.js
node /tmp/full.js
```

خروجی نشان داد نوع، برچسب، اجباری‌بودن و گزینه‌ها همه درست تجزیه می‌شوند.

### تست PDF واقعی، نه فقط پیش‌نمایش

```js
await page.emulateMedia({ media: 'print' });     // برای بررسی استایل
await page.pdf({ path:'out.pdf', printBackground:true, preferCSSPageSize:true });
```

و شمارش صفحات:

```python
import re
data = open('out.pdf','rb').read()
print('pages:', len(re.findall(rb'/Type\s*/Page[^s]', data)))
```

> **درس ۱۷:** باگ صفحهٔ خالی فقط با **تولید PDF واقعی** پیدا شد. `emulateMedia` برای بررسی
> استایل خوب است، ولی مسئلهٔ صفحه‌بندی را نشان نمی‌دهد.

### نکات ظریف تست با Playwright

| مسئله | راه‌حل |
| --- | --- |
| چک‌باکس/رادیوی پنهان قابل کلیک نیست | روی `<label>` کلیک کن: `page.locator('.doc-check:has-text("نقدی")').click()` |
| `confirm()` تست را قفل می‌کند | **قبل** از کلیک هندلر ثبت کن: `page.once('dialog', d => d.accept())` |
| `window.print()` تست را متوقف می‌کند | خنثی کن: `page.evaluate(() => { window.print = () => {} })` |
| `fill()` باگ فوکوس را نشان نمی‌دهد | از `type()` با `delay` استفاده کن |

---

## فصل ۱۲ — اگر بخواهی از صفر بسازی: نقشهٔ راه

اگر بخواهی همین را دوباره بسازی، این ترتیب را پیشنهاد می‌کنم:

### مرحله ۱ — استخراج (نیم روز)

1. venv بساز و `python-docx` نصب کن
2. متن، جدول‌ها، اندازهٔ کاغذ، رنگ سربرگ و فونت را استخراج کن
3. لوگو را بیرون بکش و Base64 کن
4. **همه‌چیز را به یک فایل متنی بریز و بخوان** — قبل از نوشتن یک خط کد

> اینجا عجله نکن. هر چیزی که اینجا از دست بدهی، بعداً به‌صورت بازنویسی برمی‌گردد.

### مرحله ۲ — مدل داده (۱ ساعت، ولی مهم‌ترین ساعت)

سازنده‌های توکن و ساختار فرم را بنویس. **قبل از هر UI.**

معیار درستی: بتوانی یک جملهٔ واقعی از فرم را با جای خالی وسطش بنویسی.

### مرحله ۳ — موتور رندر (۲ ساعت)

`renderTokens(tokens, values, editable)` را بنویس. تست کن که هر دو حالت درست کار کنند.

### مرحله ۴ — یک فرم (۲ ساعت)

فقط **یک** فرم را کامل وارد کن. تا وقتی یکی کامل کار نکند، سراغ دومی نرو.

### مرحله ۵ — گردش کار (۲ ساعت)

`advance()`، نوار مرحله‌ای، رویدادنگار.

### مرحله ۶ — چاپ (۱ ساعت)

از **همان اول** الگوی `#print-root` را استفاده کن، نه `visibility:hidden`.

### مرحله ۷ — دو فرم دیگر (۱ ساعت)

حالا که موتور آماده است، فقط داده وارد می‌کنی.

### مرحله ۸ — تقویم شمسی، امضا (۲ ساعت)

### مرحله ۹ — فرم‌ساز از صفر (۳ ساعت)

مستقیم سراغ مدل «متن روایی» برو. نسل اول را تکرار نکن.

### مرحله ۱۰ — تست و دیباگ (بی‌پایان)

بعد از هر تغییر: NUL → syntax → مرورگر.

---

### فهرست وارسی اشتباه‌های رایج

- [ ] **آیا `<!DOCTYPE html>` در ابتدای فایل هست؟** (باگ ۲ — علت ریشه‌ای)
      → با `document.compatMode` چک کن؛ باید `CSS1Compat` باشد نه `BackCompat`
- [ ] آیا عنصری که کاربر در آن تایپ می‌کند بازسازی می‌شود؟ (باگ ۵)
- [ ] آیا رنگ‌های داخل «کاغذ» از تم برنامه می‌آیند؟ (باگ ۲)
- [ ] آیا در چاپ از `visibility:hidden` استفاده شده؟ (باگ ۴)
- [ ] آیا `parseFloat` روی ورودی فارسی اجرا می‌شود؟ (باگ ۶)
- [ ] آیا وقتی وضعیت به عقب برمی‌گردد، مقادیر مرتبط پاک می‌شوند؟ (باگ ۳)
- [ ] آیا قاعدهٔ CSS جدید بعد از قاعدهٔ پایه نوشته شده؟ (باگ ۷)
- [ ] آیا عبارت باقاعدهٔ سراسری (`/g`) قبل از حلقه `lastIndex = 0` شده؟
- [ ] آیا هر مقدار کاربر که داخل HTML می‌رود از `esc()` گذشته؟
- [ ] آیا ستون محاسباتی شبیه ورودی به‌نظر می‌رسد؟ (مورد غیرباگ ۲)

---

## پیوست الف — واژه‌نامهٔ کد

| نام | چیست |
| --- | --- |
| `FORMS` | آرایهٔ سه فرم دستی‌واردشده |
| `customForm` | فرم ساخته‌شده با فرم‌ساز (یا `null`) |
| `getForm(id)` | فرم را از `FORMS` یا `customForm` پیدا می‌کند |
| `store` | نگاشت `formId → instance`، در localStorage |
| `activeFormId` | تب فعال (یا `"__scratch__"` برای فرم‌ساز) |
| `activeView` | `'run'` یا `'builder'` |
| `renderTokens(t,v,editable)` | **موتور اصلی رندر** |
| `renderPaper(form, inst, forPrint)` | بدنهٔ سند |
| `renderPrintPaper(form, inst)` | نسخهٔ چاپی کامل |
| `advance(form, inst)` | یک قدم جلو در گردش کار |
| `stageComplete(stage, values)` | آیا مرحله آمادهٔ ارسال است |
| `parseScratchText(text, i)` | متن + نشانه → توکن‌ها |
| `buildFormFromDraft()` | پیش‌نویس فرم‌ساز → شیء فرم واقعی |
| `toJalali(gy,gm,gd)` | میلادی → شمسی |
| `faToEn(s)` | ارقام فارسی → انگلیسی |
| `fa(n)` | ارقام انگلیسی → فارسی |
| `esc(s)` | فرار HTML |
| `injectPageSize(mm)` | تزریق `@page` |

### قواعد نام‌گذاری در DOM

| صفت | معنی |
| --- | --- |
| `data-fkey` | کلید فیلد متنی |
| `data-ckey` | کلید تک‌انتخابی |
| `data-mkey` | کلید چندانتخابی |
| `data-sigfile` | ورودی فایل امضا |
| `data-irow` / `data-icol` | سطر/ستون جدول اقلام |
| `data-role="jdate"` | کشوی تاریخ شمسی |
| `data-role="rowtotal"` | سلول جمع خودکار |
| `data-decide` | دکمهٔ تصمیم |
| `data-si` | شمارهٔ مرحله در فرم‌ساز |

---

## پیوست ب — دستورهای پرکاربرد

```bash
# ── استخراج از Word ──────────────────────────────────
python3 -m venv /tmp/docxenv
/tmp/docxenv/bin/pip install python-docx --quiet
unzip -l file.docx | grep -i media          # فهرست تصاویر
unzip -p file.docx word/media/image1.png > logo.png
unzip -p file.docx word/document.xml | grep -oE '<w:t[^>]*>[^<]*</w:t>' | sed 's/<[^>]*>//g'

# ── بررسی سلامت ──────────────────────────────────────
python3 -c "print('NUL:', b'\x00' in open('forms-demo.html','rb').read())"
grep -ac "function" forms-demo.html          # -a برای فایل مشکوک به دودویی

# ── بررسی نحو ────────────────────────────────────────
node --check /tmp/extracted.js

# ── تست مرورگر ───────────────────────────────────────
node test-script.mjs

# ── شمارش صفحات PDF ─────────────────────────────────
python3 -c "
import re; d=open('out.pdf','rb').read()
print('pages:', len(re.findall(rb'/Type\s*/Page[^s]', d)))"
```

---

## جمع‌بندی — پنج اصلی که همه‌چیز را شکل داد

۱. **مدل داده را قبل از رابط کاربری درست کن.** فرم‌ساز نسل اول شکست خورد چون مدل داده‌اش
   «لیست فیلد» بود، نه «متن با سوراخ». وصله زدن به رابط، این را حل نمی‌کرد.

۲. **یک تابع رندر، دو حالت.** واگرایی بین «آنچه می‌بینی» و «آنچه چاپ می‌شود» نه با دقت، بلکه
   با **ساختار** حذف شد.

۳. **کاغذ یک جسم خارجی است.** هر چیزی که نمایندهٔ دنیای فیزیکی است باید از تم برنامه ایزوله
   باشد.

۴. **قبل از رفع، بازتولید کن.** هر باگ را اول با اتوماسیون بازتولید کردم و مقادیر محاسبه‌شده
   را دیدم. دو مورد این‌طوری معلوم شد که اصلاً باگ نبودند.

۵. **بعد از هر ویرایش، سه لایه چک کن.** سلامت فایل → نحو → رفتار مرورگر. ارزان است و جلوی
   انباشت خرابی را می‌گیرد.

---

## پی‌نوشت — درسی که موقع نوشتن همین جزوه گرفتم

این جزوه قرار بود فقط مستندسازی کارِ تمام‌شده باشد. اما وقتی برای نوشتن **فصل الف (HTML)**
ساختار واقعی فایل را بررسی کردم، دیدم `<!DOCTYPE html>` ندارد و مرورگر در حالت Quirks است —
و با یک آزمایش کنترل‌شده ثابت شد که **همان، علت ریشه‌ای باگ ۲ بوده** که هفته‌ها قبل با
`color-scheme` علامتش را درمان کرده بودم.

اصلاحش کردم، کل تست‌ها را دوباره گرفتم، عارضهٔ جانبی‌اش (سرریز صفحه در چاپ) را هم برطرف کردم.

> **درس آخر:** نوشتن مستندات فقط ثبت گذشته نیست — **خودش یک ابزار پیدا کردن باگ است.**
> وقتی مجبور می‌شوی توضیح بدهی «چرا این‌طور کار می‌کند»، جاهایی که واقعاً نمی‌دانی چرا کار
> می‌کند خودشان را نشان می‌دهند.

---

## پیوست ج — ویرایش فرم‌های آماده بدون دست‌زدن به سند اصلی (لایهٔ Override)

بعد از این‌که لوگوی قابل‌تعویض برای فرم‌ساز از صفر اضافه شد، درخواست بعدی این بود: همین قابلیت
برای **سه فرم آماده** (TOA-23/28/29) هم باشد، و در کل بشود روی آن‌ها «یک ادیت کوچک» انجام داد —
عنوان، کد، شمارهٔ ویرایش، اندازهٔ کاغذ، نام نقش‌های هر مرحله.

### چرا نمی‌شد مستقیم آرایهٔ `FORMS` را دستکاری کرد

سه فرم نمونه، **بازتولید دقیق سند واقعی Word** هستند — همان چیزی که فصل ۹ با استخراج از
`python-docx` ساخته شد. اگر کاربر مستقیماً `FORMS[0].title = "..."` را عوض کند:

- با هر تازه‌سازی صفحه (`localStorage` نداشتن ورژن) دوباره سند اصلی جای آن می‌نشیند و ویرایش
  گم می‌شود، **یا** برعکس اگر در `localStorage` ذخیره شود دیگر راه بازگشت به نسخهٔ اصلی وجود
  ندارد.
- هیچ راهی برای «این کاربر خاص فرم را برای پروژهٔ خودش تغییر داد» در برابر «این سند مرجع رسمی
  شرکت است» وجود نخواهد داشت.

**تصمیم:** سند مرجع دست‌نخورده می‌ماند؛ تغییرات کاربر در یک لایهٔ جدا نگه داشته می‌شود و فقط در
لحظهٔ خواندن روی سند مرجع «اعمال» می‌شود. همان الگویی که خیلی از سیستم‌های واقعی (مثلاً
تنظیمات پیش‌فرض + override کاربر در ابزارهای CI) استفاده می‌کنند.

### طراحی: یک شیء override به‌جای هزار پرچم

```js
const OVERRIDE_FIELDS = ["title","code","rev","logo","pageKey"];

function getOverrides(formId){
  store.__overrides = store.__overrides || {};
  return store.__overrides[formId] || null;
}
function setOverride(formId, key, value){
  store.__overrides = store.__overrides || {};
  const ov = store.__overrides[formId] = store.__overrides[formId] || {};
  if (value === null || value === "") delete ov[key]; else ov[key] = value;
  if (!Object.keys(ov).length) delete store.__overrides[formId];   // شیء خالی را نگه نداریم
  saveStore();
}
function applyOverrides(base, ov){
  const out = {...base};
  OVERRIDE_FIELDS.forEach(k=>{ if (ov[k] != null && ov[k] !== "") out[k] = ov[k]; });
  if (ov.title) out.shortTitle = ov.title.slice(0,14);
  if (ov.pageKey && SCRATCH_PAGE_PRESETS[ov.pageKey]){
    out.pageSize  = SCRATCH_PAGE_PRESETS[ov.pageKey].size;
    out.pageLabel = SCRATCH_PAGE_PRESETS[ov.pageKey].label;
  }
  if (ov.roles){
    out.stages = base.stages.map(st => ov.roles[st.key] ? {...st, role: ov.roles[st.key]} : st);
  }
  return out;
}

function getForm(id){
  if (customForm && customForm.id === id) return customForm;
  const base = FORMS.find(f=>f.id===id);
  if (!base) return null;
  const ov = getOverrides(id);
  return ov ? applyOverrides(base, ov) : base;   // بدون override، همان مرجع اصلی برمی‌گردد
}
```

نکات طراحی:

- **`applyOverrides` هرگز `base` را جهش (mutate) نمی‌کند** — با `{...base}` یک کپی می‌سازد. اگر
  مستقیم `base.title = ov.title` می‌نوشتیم، چون `base` همان آبجکت داخل `FORMS` است، سند مرجع
  برای همیشه آلوده می‌شد.
- **حذف کلید خالی، نه ذخیرهٔ رشتهٔ خالی.** وقتی کاربر عنوان را کامل پاک می‌کند، `setOverride`
  کلید را از شیء `ov` حذف می‌کند تا `getForm` به‌طور طبیعی به مقدار اصلی برگردد — به‌جای این‌که
  یک عنوان خالی نمایش داده شود.
- **نام نقش‌ها جدا از بقیهٔ فیلدها مدیریت می‌شود** (`setRoleOverride` / `ov.roles`) چون یک
  شیء تودرتوست، نه یک مقدار ساده — الگوی یکسان اما با یک لایهٔ اضافه.

این یعنی همان الگوی `form.logo || LOGO_SRC` که در لوگوی فرم‌ساز دیدیم، اینجا یک پله
عمومی‌تر شده: **به‌جای این‌که هرجا از فرم استفاده می‌شود جداگانه چک کنیم آیا override هست،
یک نقطهٔ ورودی واحد (`getForm`) ساختیم که همیشه نسخهٔ نهایی (اصلی + رویهم‌گذاری‌شده) را
برمی‌گرداند.** هر بخش دیگر کد فقط باید `getForm(id)` را صدا بزند و نگران override بودن یا
نبودن نباشد.

### پنل «ویرایش سریع این فرم» — فقط برای فرم‌های نمونه

فرم‌ساز از صفر جای خودش را دارد (فرم را از ابتدا می‌سازی)؛ این پنل برای **ویرایش سبک روی سند
موجود** است، پس فقط وقتی نمایش داده می‌شود که فرم فعال یکی از سه فرم آماده باشد:

```js
const isSample = FORMS.some(f => f.id === form.id);
```

اگر `hasOverrides(form.id)` باشد، یک نشان «ویرایش‌شده» و دکمهٔ «بازگشت کامل به فرم اصلی»
(که `clearOverrides` را صدا می‌زند) هم نشان داده می‌شود.

**نکتهٔ فوکوس (تکرار درس ۱۲ در جای جدید):** فیلدهای متنی (عنوان/کد/ویرایش) هنگام تایپ **نباید**
`render()` کامل صدا بزنند — دقیقاً همان درسی که در جدول اقلام یاد گرفتیم. راه‌حل اینجا یک
تابع سبک‌تر بود که فقط جاهایی را که واقعاً باید زنده به‌روز شوند (برچسب تب، آمار کناری) با
`textContent` عوض می‌کند، بدون بازسازی کل صفحه:

```js
if (t.id === "ed-title"){ setOverride(activeFormId, "title", t.value); refreshEditEchoes(); return; }

function refreshEditEchoes(){
  const form = getForm(activeFormId);
  if (!form) return;
  const stats = document.querySelectorAll(".builder-side .stat b");
  if (stats[0]) stats[0].textContent = form.code;
  if (stats[1]) stats[1].textContent = fa(form.rev);
  const tab = document.querySelector(`[data-formtab="${CSS.escape(activeFormId)}"]`);
  if (tab) tab.textContent = `${form.code} — ${form.shortTitle}`;
}
```

فیلدهایی که با کلیک/انتخاب عوض می‌شوند (لوگو، اندازهٔ کاغذ، نام نقش) مشکل فوکوس ندارند، پس
همان `render()` کامل صدا زده می‌شود — همیشه لازم نیست بهینه‌کاری کنی، فقط جایی که واقعاً یک
`<input>` متنی زیر دست کاربر است.

### باگ ۸ — بعد از بارگذاری مجدد صفحه، برچسب تب به نسخهٔ اصلی برمی‌گشت

**علامت:** با اسکریپت تست `regress8-edit.mjs` عنوان فرم TOA-23 را عوض کردم، صفحه را
`page.reload()` کردم، و برچسب تب باید ویرایش‌شده باشد — ولی نبود:

```
tab after reload (persisted): TOA-23 — مأموریت      ← انتظار: TOA-23-B — درخواست مأموری
```

در حالی که خودِ سند (نمای اجرا، چاپ) کاملاً درست override را نشان می‌داد و در `localStorage`
هم ذخیره شده بود.

**تشخیص:** چون فقط تب اشتباه بود نه بقیهٔ صفحه، مشکل باید در تابعی باشد که **مخصوص ساخت
تب‌هاست** و از یک منبع داده متفاوت می‌خواند.

**علت ریشه‌ای:** `renderTabs()` مستقیم روی آرایهٔ خام `FORMS` پیمایش می‌کرد، نه از `getForm()`
عبور می‌کرد — یعنی درست همان نقطه‌ای که override باید اعمال شود را دور می‌زد:

```js
// قبل — عنوان خام FORMS را می‌خواند، از کنار override رد می‌شود
let tabs = FORMS.map(f=>
  `<button ... data-formtab="${f.id}">${esc(f.code)} — ${esc(f.shortTitle)}</button>`
).join("");
```

**اصلاح:** هر آیتم را قبل از رندر از `getForm()` عبور دادم:

```js
// بعد — هر آیتم از getForm عبور می‌کند، پس override را می‌بیند
let tabs = FORMS.map(f0=>{
  const f = getForm(f0.id);
  return `<button type="button" class="formtab ${f.id===activeFormId?'active':''}"
    data-formtab="${f.id}">${esc(f.code)} — ${esc(f.shortTitle)}</button>`;
}).join("");
```

> **درس ۱۸:** وقتی یک تابع «نقطهٔ ورودی واحد» می‌سازی (اینجا `getForm`)، باید **همهٔ** جاهایی
> که قبلاً مستقیم از منبع خام می‌خواندند را پیدا کنی و به آن نقطهٔ واحد وصل کنی — نه فقط
> جاهایی که تازه اضافه کرده‌ای. `grep -n "FORMS\."` (یا در این مورد `FORMS.map`/`FORMS.find`)
> بعد از ساختن یک لایهٔ جدید، ارزان‌ترین راه پیدا کردن این‌جور نقطه‌های جاافتاده است.

با اجرای دوبارهٔ `regress8-edit.mjs` بعد از این اصلاح، هر ۱۰ بررسی (فوکوس هنگام تایپ، به‌روزرسانی
زندهٔ تب، ماندگاری بعد از reload، آپلود و ریست لوگو، تغییر اندازهٔ کاغذ، بازتاب در نمای اجرا و
PDF، بازگشت کامل، و عدم تأثیر روی فرم‌های دیگر) بدون خطا (`ERRORS: []`) تأیید شد.

---

## پیوست د — فرم چهارم: بازتولید دقیق یک برگهٔ اکسل (گزارش روزانهٔ کارگاه)

سه فرم اول از فایل‌های **Word** آمده بودند؛ فرم چهارم («03  05  1405.xlsx») یک **برگهٔ اکسل** بود —
گزارش روزانهٔ کارگاه با ساختاری کاملاً متفاوت: نه یک نامهٔ اداری با جای خالی، بلکه یک **شبکهٔ
جدولی متراکم** با دو جفت جدولِ کنارهم و فرمول‌های محاسباتی. این پیوست مستند می‌کند که استخراج
و بازتولید اکسل چه تفاوت‌هایی با Word داشت و چه اشتباهی اول مرتکب شدم.

### استخراج ساختار با openpyxl

برای Word از `python-docx` استفاده کرده بودیم؛ برای اکسل ابزار معادل `openpyxl` است:

```python
import openpyxl
wb = openpyxl.load_workbook("03  05  1405.xlsx", data_only=True)  # data_only: نتیجهٔ فرمول، نه خود فرمول
ws = wb["1"]                                  # برگهٔ اصلی
ws.sheet_view.rightToLeft                     # True — برگه راست‌به‌چپ است
ws.print_area                                 # '1'!$A$1:$O$52 — محدودهٔ چاپ = مرز واقعی فرم
ws.merged_cells.ranges                        # ۱۰۸ ادغام — نقشهٔ هندسهٔ فرم
for row in ws.iter_rows(min_row=1, max_row=60):
    ...                                       # مقدار سلول‌ها
for col in "ABCDEFGHIJKLMNO":
    ws.column_dimensions[col].width           # عرض ستون‌ها — برای بازتولید نسبت‌ها
```

سه خروجی کلیدی این تحلیل:

۱. **محدودهٔ چاپ** (`print_area`) مرز واقعی فرم را می‌گوید — نه `max_row`/`max_col` که شامل
   سلول‌های زائد (فهرست‌های کمکی ستون T، داده‌های تست) هم می‌شود.
۲. **نقشهٔ ادغام‌ها** عملاً «طرح فرم» است: از روی همین ۱۰۸ ادغام فهمیدم که تجهیزات (A9:I26) و
   نیروی کار (J9:O26) دو جدولِ **کنار هم در یک شبکه‌اند**، نه دو جدول مستقل.
۳. **تفکیک دادهٔ نمونه از ساختار فرم**: سلول‌ها پر از دادهٔ یک روز واقعی بودند (کانکس ۶ عدد،
   میلگرد ۱۱٬۷۴۶ کیلو…). ساختار = برچسب‌ها و ادغام‌ها؛ داده = چیزی که باید فیلد خالی شود.

### اشتباه اول: «بازنویسی» به‌جای «بازتولید»

نسخهٔ اولم ساختار را به زبان خود دمو ترجمه کرد: سربرگ را متن روایتی کردم (مثل نامه‌های TOA)،
چهار جدول را زیر هم چیدم با دکمهٔ «افزودن ردیف»، و سربرگ استاندارد کد/لوگو را هم بالایش گذاشتم.
تمیز بود، کار می‌کرد — و **رد شد**: «دقیقا باید شبیه به همون فایل اکسل باشه، بدون هیچ تغییری».

> **درس ۱۹:** وقتی مرجع، یک سند واقعیِ در حال استفاده است، «بهتر کردن» چیدمانش خیانت به مرجع
> است. کاربر آن برگه را هر روز می‌بیند؛ هر انحرافی — حتی منطقی — اعتماد به دمو را می‌شکند.
> اول عین سند را بساز، بعد اگر خواستند، بهبود پیشنهاد بده.

### معماری نسخهٔ درست: یک جدول ۱۵ ستونه، آینهٔ شبکهٔ اکسل

به‌جای کش دادن ماشین «جدول عمومی»، یک رندرر مخصوص نوشتم (`renderDailyReportSheet`) که همان
شبکهٔ اکسل را سلول‌به‌سلول می‌سازد:

- **یک `<table>` با ۱۵ ستون** (A تا O) و `<colgroup>`ای که عرض هر ستون را به همان نسبت
  عرض‌های اکسل می‌دهد. چون برگه RTL است، ستون A خودبه‌خود راست‌ترین ستون می‌شود.
- **ادغام‌ها با colspan/rowspan** عیناً بازسازی شدند — مثلاً «وضعیت کارگاه:» با
  `colspan="2" rowspan="2"`، و بلوک «جمع کل» نیروی کار با `rowspan="4"` دقیقاً مثل J23:L26.
- **دو جدول کنار هم = ردیف‌های مشترک:** ردیف‌های ۱۱ تا ۲۲ هم سلول‌های تجهیزات را دارند هم
  نیروی کار؛ ردیف‌های ۲۳ تا ۲۵ فقط تجهیزات (سمت دیگر زیر rowspan جمع کل است). تعداد ردیف‌ها
  **ثابت** است (۱۵/۱۲/۹/۹) — مثل کاغذ واقعی، بدون دکمهٔ افزودن ردیف.
- **فرم بدون سربرگ استاندارد:** پرچم `sheet` روی تعریف فرم باعث می‌شود `renderDocHeader`
  (کد/ویرایش/لوگوی TOA) اصلاً رندر نشود — چون در اکسل واقعی وجود ندارد.
- **فرمول‌های اکسل → سلول‌های محاسباتی:** «عدم حضور = کل − حضور»، «مانده مصالح = مقدار − مصرف»
  و ردیف‌های جمع کل، با همان الگوی به‌روزرسانی هدفمند `textContent` (درس ۱۲: هرگز DOM زیر
  دست کاربر را بازسازی نکن).
- **گردش کار بدون ردپا در چاپ:** دو مرحلهٔ workflow (سرپرست کارگاه → بازبینی مدیر پروژه) با
  `runs: []` تعریف شدند؛ در نمای زنده دکمهٔ تصمیم دارند ولی در چاپ هیچ‌چیز اضافه نمی‌کنند —
  خروجی PDF فقط خود شبکهٔ اکسل است.

یک ریزه‌کاری مهاجرت: چون `localStorage` کاربر ممکن بود از نسخهٔ قبلی جدول‌های ۱ ردیفه داشته
باشد و رندرر جدید به ۱۵ ردیف ثابت نیاز دارد، `getInstance` حالا شکل جدول‌ها را چک می‌کند و
اگر ناسازگار بود فقط جدول‌ها را از نو می‌سازد (مقادیر سربرگ حفظ می‌شوند).

### سه تصویر گم‌شدهٔ سربرگ — جایی که openpyxl دروغ گفت

بعد از تحویل، کاربر گفت: «اون سه تا عکس روی سربرگ رو نداره.» ولی openpyxl گفته بود
`ws._images` خالی است! تصویرها کجا بودند؟

فایل xlsx هم مثل docx یک ZIP است. باز کردنش نشان داد:

```
xl/media/image1.png   ← لوگوی مشاور
xl/media/image2.png   ← لوگوی تحکیم عمران (پیمانکار)
xl/media/image3.jpg   ← آرم وزارت راه و شهرسازی (کارفرما)
xl/drawings/drawing2.xml   ← مختصات لنگر هر تصویر
```

`drawing2.xml` می‌گوید هر تصویر به کدام سلول لنگر شده (`<xdr:from><xdr:col>0</xdr:col>…`) و
ابعادش چقدر است (به واحد EMU؛ هر سانتی‌متر = ۳۶۰٬۰۰۰ EMU). هر سه تصویر روی **ردیف ۱** شناور
بودند — همان ردیفی که چون «هیچ متن سلولی نداشت» در استخراج اولیه نادیده‌اش گرفته بودم:
تحکیم عمران در ستون‌های A-B (راست)، وزارت راه در F-J (وسط)، مشاور در M-O (چپ).

یک نکتهٔ ظریف‌تر: تصویر مشاور در XML یک برش داشت —
`<a:srcRect t="29540" b="29540"/>` یعنی «۲۹.۵۴٪ از بالا و ۲۹.۵۴٪ از پایین را نمایش نده».
اگر تصویر خام را می‌گذاشتم، چیزی متفاوت از چاپِ اکسل دیده می‌شد. با Pillow همان برش را
اعمال کردم و بعد base64 کردم:

```python
from PIL import Image
im = Image.open("image1.png"); w, h = im.size
top = round(h * 0.29540)
im.crop((0, top, w, h - top)).save("dr-logo-left.png")
```

> **درس ۲۰:** «کتابخانه گفت چیزی نیست» یعنی «کتابخانه آن را نمی‌بیند»، نه «وجود ندارد».
> فایل‌های آفیس ZIP هستند — وقتی چیزی گم است، خود ZIP را باز کن و XML خام را بخوان.
> و به ردیف‌های «خالی» مشکوک باش: خالی از متن ≠ خالی از محتوا.

---

## پیوست هـ — سیستم خطای فیلدهای اجباری: سه دور بازخورد واقعی

این پیوست شاید مهم‌ترین درس محصولی کل پروژه را دارد: **دکمهٔ غیرفعالِ بی‌توضیح، یک بن‌بست
است** — و اینکه یک مشکل UX معمولاً با اولین اصلاح حل نمی‌شود؛ با گفت‌وگوی رفت‌وبرگشتی با
کاربر واقعی حل می‌شود. سه دور طول کشید.

### دور ۱ — «نمی‌توانم ثبت را بزنم و نمی‌فهمم چرا»

**مشکل:** دکمهٔ «ثبت و ارجاع» تا کامل شدن فیلدهای اجباری `disabled` بود. کاربر همه‌چیز را
(به خیال خودش) پر کرده بود، دکمه کار نمی‌کرد، و هیچ سرنخی نبود که چه چیزی مانده.

**اصلاح:** حذف کامل حالت غیرفعال. دکمه همیشه قابل کلیک است؛ اگر چیزی ناقص باشد، کلیک به‌جای
پیشروی، **خطا را نشان می‌دهد**:

```js
function missingRequired(form, inst){        // فهرست فیلدهای اجباریِ خالیِ مرحلهٔ جاری، با برچسب
  ...
  if (inst.stageIndex === 0) (form.requiredValues||[]).forEach(r => need(r.key, r.label));
  (st.runs||[]).forEach(tok=>{
    if ((tok.k==='f'||tok.k==='sig'||tok.k==='c') && tok.required) need(tok.key, tok.label);
  });
  if (st.decision && !values[st.decision.key]) list.push({..., decision: true});
}

// در هندلر کلیک:
if (e.target.id === "advanceBtn"){
  const missing = missingRequired(form, inst);
  if (missing.length){
    attemptedAdvance[activeFormId] = true;        // از حالا به بعد خطاها را زنده نشان بده
    updateActionBarOnly(form, inst);              // جعبهٔ قرمز با فهرست نام فیلدها
    document.querySelector(".field-missing")?.scrollIntoView({behavior:"smooth", block:"center"});
    return;
  }
  advance(form, inst); ...
}
```

سه جزء کنار هم: جعبهٔ قرمز بالای دکمه (فهرست نام فیلدها)، قاب قرمز روی خود فیلدها در سند
(`applyMissingHighlights` — فیلد متنی، سه‌کشویی تاریخ، دکمهٔ آپلود امضا، گزینه‌ها و حتی
دکمه‌های تصمیم)، و اسکرول خودکار به اولین مورد. پرچم `attemptedAdvance` باعث می‌شود خطاها
فقط **بعد از اولین تلاش** ظاهر شوند (نه از همان لحظهٔ ورود به فرم) و با پر شدن هر فیلد،
زنده از فهرست و هایلایت حذف شوند.

### دور ۲ — «پرش کردم ولی باز خطا می‌دهد» (فیلد بی‌برچسب)

**مشکل:** خطا می‌گفت «تاریخ گزارش» و کاربر مطمئن بود تاریخ را پر کرده. ریشه: در اکسلِ اصلی،
خانهٔ تاریخ گزارش یک سلول کوچکِ **بی‌برچسب** کنار «وضعیت کارگاه» است. کاربر «تاریخ قرارداد»
(که برچسب دارد) را پر کرده بود و قاب قرمزِ تنها روی یک خانهٔ بی‌نام، برای پیدا کردنش کافی نبود.

**اصلاح:** برچسب شناور روی خود فیلد — `data-missing-label` + شبه‌عنصر CSS:

```js
const mark = (el, label) => {
  el.classList.add("field-missing");
  if (label) el.dataset.missingLabel = label;    // نام فیلد، چسبیده به خودش
};
```

```css
.field-missing[data-missing-label]::after{
  content:attr(data-missing-label);              /* متن برچسب از خود اتریبیوت */
  position:absolute; top:-10px; inset-inline-start:-2px;
  background:#C0392B; color:#fff; font-size:9.5px; ...}
```

**باگ فرعی همین دور:** برچسب رندر می‌شد ولی **بریده** دیده می‌شد. علت: سلول‌های شبکهٔ اکسلی
`overflow:hidden` داشتند (برای مهار متن بلند) و شبه‌عنصری که بیرون سلول می‌نشست، قیچی می‌شد.
اصلاح یک‌خطی: `table.dr-sheet td.field-missing{overflow:visible}` — فقط وقتی سلول در حالت
خطاست، سرریزش آزاد شود.

> **درس ۲۱:** پیام خطا باید تا خود فیلد سفر کند. «فلان چیز خالی است» در پایین صفحه، وقتی
> فیلد در سندی شلوغ و بی‌برچسب گم شده، عملاً هیچ کمکی نیست — نام را بچسبان به خود خانه.

### دور ۳ — «باز هم خطا می‌دهد!» (کشوی «سال» که شبیه برچسب بود)

**مشکل:** کاربر متن صفحه‌اش را فرستاد و جواب داخلش بود: «۲۰ / شهریور / **سال**». روز و ماه
انتخاب شده بود، ولی کشوی سوم هنوز روی گزینهٔ پیش‌فرضش بود — و کلمهٔ «سال» آن‌قدر شبیه برچسب
است که کاربر مطمئن بود تاریخ کامل است. تاریخِ نیمه‌پر هم از نظر سیستم = خالی.

**اصلاح (دو لایه):**

۱. **حذف خود مشکل — سال خودکار:** وقتی روز و ماه انتخاب شد و سال خالی است، سال جاری خودکار
   انتخاب می‌شود (در گزارش روزانه تقریباً همیشه سال جاری است؛ کاربر همچنان می‌تواند عوضش کند):

```js
function autoFillJalaliYear(group, vals){
  if (!vals.d || !vals.m || vals.y) return;          // فقط وقتی روز+ماه هست و سال نیست
  const [cjy] = todayJalaliParts();
  const ySel = [...group].find(s=> s.dataset.part === "y");
  if (ySel && [...ySel.options].some(o=> o.value === String(cjy))){
    ySel.value = String(cjy);
    vals.y = String(cjy);
  }
}
```

   در هر دو هندلر تاریخ (`jdate` و `itemjdate`) صدا زده می‌شود، پس همهٔ تاریخ‌های شمسی دمو
   یک‌دست همین رفتار را دارند.

۲. **اگر باز هم ناقص ماند، بگو دقیقاً کدام قسمت:** موقع هایلایت، کشوهای خالیِ همان تاریخ
   پیدا و قرمز می‌شوند و برچسب شناور دقیق‌تر می‌شود:

```js
const emptyParts = dateSelects.filter(s=> !s.value);
emptyParts.forEach(s=> s.classList.add("jd-missing"));   // خود کشوی خالی قرمز
if (emptyParts.length && emptyParts.length < 3)
  label += " — فقط " + emptyParts.map(s=> partNames[s.dataset.part]).join(" و ") + " انتخاب نشده";
// → «تاریخ گزارش — فقط ماه و سال انتخاب نشده»
```

> **درس ۲۲:** تست خودکار ثابت می‌کند مسیر درست کار می‌کند؛ فقط کاربر واقعی نشان می‌دهد
> مسیر درست **پیدا نمی‌شود**. هر سه دور این پیوست از گزارش کاربر واقعی آمد، نه از تست —
> و ارزشمندترین گزارش، متن خامی بود که کاربر از صفحه‌اش کپی کرد و «سال» انتخاب‌نشده
> داخلش دیده می‌شد. برای دیباگ UX، از کاربر بخواه همان چیزی که می‌بیند را بفرستد.

### جمع‌بندی الگو

```
دکمهٔ همیشه‌فعال
  → کلیک ناموفق: جعبهٔ فهرست خطا + قاب قرمز + برچسب نام روی خود فیلد + اسکرول
    → پر شدن هر فیلد: حذف زندهٔ همان مورد از فهرست و هایلایت
      → ورودی چندجزئی (تاریخ): جزء جاافتاده را نام ببر و خودش را قرمز کن
        → و بهتر از همه: جزئی که تقریباً همیشه یک جواب دارد را خودکار پر کن
```

این الگو سراسری است: چهار فرم آماده، فرم‌های فرم‌ساز، مراحل تصمیم — همه بدون حتی یک خط کد
اختصاصی per-form، چون همه از همان `missingRequired` و توکن‌های `required` تغذیه می‌شوند.
